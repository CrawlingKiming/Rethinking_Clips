\documentclass[11pt]{article}

\usepackage[margin=1in]{geometry}
\usepackage{amsmath,amssymb,amsthm}
\usepackage{booktabs}
\usepackage{graphicx}
\usepackage{float}
\IfFileExists{microtype.sty}{\usepackage{microtype}}{}
\usepackage{xcolor}
\usepackage[numbers,sort&compress]{natbib}
\usepackage{hyperref}

\hypersetup{
  colorlinks=true,
  linkcolor=blue!55!black,
  citecolor=blue!55!black,
  urlcolor=blue!55!black
}

\newtheorem{theorem}{Theorem}
\newtheorem{proposition}[theorem]{Proposition}
\newtheorem{corollary}[theorem]{Corollary}
\theoremstyle{remark}
\newtheorem{remark}[theorem]{Remark}

\newcommand{\E}{\mathbb{E}}
\newcommand{\Cov}{\operatorname{Cov}}

\title{\bf When Should We Clip?\\
Effective Sample Size as a Criterion}
\author{Dongkyu Derek Cho, Thomas Voice}
\date{}

\begin{document}
\maketitle

\begin{abstract}
Clipping is widely treated as necessary for policy optimization, yet it
can also discard useful learning signal.  This tension is pronounced in
reinforcement learning with verifiable rewards, where permissive updates learn
quickly but can fail after the policy moves too far from its rollout data.  We
ask when clipping becomes useful.  We show that importance-weight concentration
governs the reliability of a permissive gradient.  We connect this effective
support to gradient error and a finite-time optimization guarantee, then derive when clipping's
variance reduction repays its bias.  An exactly evaluable contextual-bandit
testbed recovers the predicted raw-versus-clipped gradient-error crossover.
Because the exact comparison is unavailable during training, we derive a
conditional guarantee for replacing it with an observable ESS gate.  A simple
fixed threshold selects the low-ESS updates on which clipping reduces gradient
error, masks only a small fraction of updates, and substantially improves
lower-tail optimization performance.  These results identify ESS as a
practical reliability coordinate for deciding when to clip.
\end{abstract}

\section{Introduction}

Policy optimization on fixed rollout data faces a basic mismatch: the policy
being updated is no longer the policy that generated the data.  PPO addresses
this mismatch with a clipped surrogate that suppresses selected samples once
their likelihood ratios cross a prescribed boundary \citep{schulman2017ppo}.
The same mechanism is now standard in reinforcement learning with verifiable
rewards (RLVR), where an expensive rollout batch is followed by several policy
updates \citep{shao2024deepseekmath}.  Because the mismatch grows during these
updates, clipping is often treated as a necessary condition for stable
training.

RLVR practice reveals that this view is incomplete.  Clipping can remove
gradients from low-probability tokens that carry useful new behavior, slowing
early learning and contributing to entropy collapse
\citep{yu2025dapo,park2025clipping}.  More permissive objectives can avoid this
loss and improve reward faster, yet they may fail later in training; the
motivating CISPO trajectory in Figure~\ref{fig:llm-motivation} exhibits this
pattern \citep{minimax2025m1}.  These observations do not rank clipping and
permissive optimization globally.  They indicate that the preferable update
changes as a rollout batch becomes less representative of the current policy.
The central question is therefore not whether to clip, but when clipping
becomes necessary.

\begin{figure}[H]
  \centering
  \fbox{\parbox[c][1.28in][c]{0.94\linewidth}{\centering
  \textbf{LLM empirical motivation placeholder}\\[4pt]
  Insert the supplied training curves showing efficient early learning without
  PPO masking, followed by the late CISPO collapse.  Overlay sequence ESS on
  the same update axis and mark the proposed threshold at $0.1$.}}
  \caption{Empirical motivation.  A permissive update improves reward rapidly
  while the rollout batch remains representative, then fails after effective
  support deteriorates.  The final figure will report the model, benchmark,
  seeds, confidence intervals, and the onset of the ESS decline.}
  \label{fig:llm-motivation}
\end{figure}

Common diagnostics do not locate this change of regime.  A clipping threshold
specifies how to modify an individual sample, an update count records how long
the batch has been optimized, and a divergence summarizes how far the policy
has moved \citep{qi2026dppo}.  None of these quantities directly measures
whether the fixed batch still supports an accurate gradient for the current
policy.  That question depends on the concentration of the sequence importance
weights across the entire batch.

Sequence effective sample size (ESS) measures this concentration.  ESS is high
when support is distributed across many responses and low when only a few
responses carry most of the importance weight.  Our central thesis is that the
absence of clipping is not inherently unstable.  A permissive update remains
statistically reliable while its effective support is sufficient.  Clipping
becomes useful when support deteriorates enough that the variance it removes
justifies the bias it introduces.

Establishing this thesis requires a bridge from coverage to optimization.  We
derive the exact mean-squared error of the permissive gradient in terms of
effective support and gradient scale.  We then show that gradient error controls
both one-step policy improvement and finite-time convergence to a stationary
region.  Finally, we differentiate the PPO
surrogate to show that clipping is an advantage-dependent gradient mask, not
global ratio truncation, and derive the exact bias--variance condition under
which a modified gradient is preferable.  Together, these results make ESS a
reliability coordinate for locating the regime in which a safeguard becomes
worthwhile.

We test every link in an exactly evaluable verifier problem before transferring
the rule to language-model training.  The contextual-bandit study follows the
rollout and minibatch order of RLVR while exposing the population gradient and
reward.
Across 100 paired runs, ESS predicts both gradient error and the sign of the
resulting reward change.  Paired redraws at frozen policy states recover the
predicted estimator crossover: PPO masking has lower MSE when effective support
is scarce, while the permissive estimator has lower MSE when support is broad.
A disjoint calibration split then fits an oracle reference without using the
held-out outcomes and improves held-out gradient risk relative to either static
choice.  Since that oracle calculation is unavailable in RLVR, we prove the
conditional MSE guarantee supplied by an observable gate and test a
prespecified $0.1$ trigger along its own training trajectory.  It selects a
sparse region in which PPO has lower gradient error, preserves the typical
endpoint of permissive optimization, and sharply improves its lower tail while
masking only $3.3\%$ of updates.
Section~\ref{sec:llm-experiments}
specifies the matched LLM experiment needed to test whether the same regime
change appears under PPO, Clip-Higher, and DPPO safeguards.

\subsection{Related perspectives}

\paragraph{Clipping in RLVR.}
PPO introduced clipping as a local policy-update mechanism
\citep{schulman2017ppo}, and GRPO carried this surrogate into RLVR
\citep{shao2024deepseekmath}.  Recent methods expose the cost of applying the
same constraint throughout training.  DAPO relaxes the upper boundary,
Clip-Low changes entropy behavior, CISPO retains gradients beyond the PPO
boundary, and DPPO replaces ratio clipping with a divergence-based constraint
\citep{yu2025dapo,park2025clipping,minimax2025m1,qi2026dppo}.  These methods
change the intervention; our question is when an intervention is statistically
warranted.

\paragraph{Reliability under policy mismatch.}
Importance-sampling analyses use second moments and ESS to quantify coverage
\citep{metelli2018pois,metelli2020is}.  Recent LLM methods likewise adapt to
weight concentration, ESS, or sequence-level mismatch
\citep{zheng2025m2po,shen2026vespo,huang2026vcpo,fakoor2026p3o}.  Our analysis
adds the missing optimization link: it connects sequence ESS to gradient MSE,
then connects that MSE to finite-time policy optimization and the clipping
crossover.

\subsection{Contributions}

\begin{enumerate}
\item We derive an exact connection between sequence ESS, effective support,
and the mean-squared error of a fixed-rollout policy-gradient estimator.
\item We connect gradient error to one-step improvement and finite-time
stationarity, then derive the exact bias--variance condition under which
clipping improves the estimator.
\item We derive the conditional guarantee of an observable ESS gate, recover
the predicted crossover in a contextual-bandit verifier task, and show that a
sparse fixed threshold improves the lower tail of repeated optimization.
\end{enumerate}

The next section states the fixed-rollout problem in which this reliability
question can be analyzed exactly.

\section{Preliminaries: fixed-rollout policy optimization}
\label{sec:setting}

RLVR alternates between data collection and optimization.  A rollout policy
generates a batch of complete responses, and a changing current policy is then
updated on that fixed batch.  We analyze one such optimization phase and keep
the rollout policy explicit throughout.

\subsection{Population target and sampled update}

Let $X\sim\nu$ be a prompt, let $Q(\cdot\mid X)$ be the rollout policy, and let
$R(X,Y)$ be the terminal verifier reward of a complete response.  During the
optimization phase, the policy of interest is $P_\theta$, with objective
$J(\theta):=\E_{P_\theta}[R]$.  Assuming $P_\theta\ll Q$, this target can be
evaluated from rollout data using the sequence likelihood ratio
\begin{equation}
  W_\theta(X,Y)
  :=\frac{P_\theta(Y\mid X)}{Q(Y\mid X)},
  \qquad
  \E_Q[W_\theta\mid X]=1.
  \label{eq:weight}
\end{equation}

The policy gradient separates the response-level learning signal from the
distribution mismatch.  Let $b(X)$ be a detached prompt-only baseline, set
$A=R-b(X)$, and define the current-policy score and gradient contribution by
\begin{equation}
  S_\theta:=\nabla_\theta\log P_\theta(Y\mid X),
  \qquad
  H_\theta:=A S_\theta.
  \label{eq:score}
\end{equation}
Assuming that differentiation and expectation can be interchanged, the target
gradient is
\begin{align}
  g(\theta):=\nabla J(\theta)
  &=\E_{P_\theta}[R S_\theta]
    \label{eq:policy-gradient-1}\\
  &=\E_{P_\theta}[(R-b(X))S_\theta]
    \label{eq:policy-gradient-2}\\
  &=\E_{P_\theta}[H_\theta]
    \label{eq:policy-gradient-3}\\
  &=\E_Q[W_\theta H_\theta].
  \label{eq:true-gradient}
\end{align}
The first line is the score-function identity.  The second uses
$\E_{P_\theta}[b(X)S_\theta\mid X]
=b(X)\nabla_\theta\sum_yP_\theta(y\mid X)=0$.  The third substitutes
\eqref{eq:score}, and the fourth changes measure from $P_\theta$ to $Q$.
Given $N$ independent prompt-response pairs from the rollout distribution, the
permissive importance-weighted estimator is
\begin{equation}
  \widehat g_W(\theta)
  :=\frac1N\sum_{i=1}^N W_{\theta,i}H_{\theta,i}.
  \label{eq:raw-estimator}
\end{equation}
Equation~\eqref{eq:true-gradient} identifies the desired update, whereas
Equation~\eqref{eq:raw-estimator} describes what the fixed batch can estimate.
The estimator is unbiased at fixed $\theta$, but its accuracy can deteriorate as
$P_\theta$ moves into responses that are poorly represented under $Q$.  We next
quantify this loss of coverage.

\subsection{Effective support of the rollout distribution}

Sequence ESS summarizes how much support the rollout distribution provides for
the current policy.  Assuming $\E_Q[W_\theta^2]<\infty$, define the normalized
population ESS by
\begin{equation}
  \rho_\theta:=\frac{1}{\E_Q[W_\theta^2]}
  =\exp\{-D_2(P_\theta\|Q)\}.
  \label{eq:population-ess}
\end{equation}
Because $\E_Q[W_\theta]=1$, its relation to ratio dispersion is exact:
\begin{align}
  \E_Q[(W_\theta-1)^2]
  &=\E_Q[W_\theta^2]-2\E_Q[W_\theta]+1
    \label{eq:weight-variance-proof-1}\\
  &=\E_Q[W_\theta^2]-1
    \label{eq:weight-variance-proof-2}\\
  &=\rho_\theta^{-1}-1.
    \label{eq:weight-variance}
\end{align}
The first line expands the square, the second uses $\E_Q[W_\theta]=1$, and the
third uses Equation~\eqref{eq:population-ess}.  Hence $N\rho_\theta$ is the
population effective sequence count, consistent with the second-moment
coverage principle used in importance-sampling policy optimization
\citep{metelli2018pois,metelli2020is}.  When $\rho_\theta$ is near one, the batch
retains nearly all of its nominal support.  When it approaches zero, a batch of
$N$ responses behaves as if it contained far fewer effective observations.

ESS measures ratio concentration, not the size of the gradient contributions
attached to those ratios.  The theoretical analysis begins by separating these
two quantities and then asks whether their joint effect is large enough to
change the preferred policy update.

\section{Theoretical analysis}
\label{sec:theory}

Section~\ref{sec:setting} reduced fixed-rollout optimization to a sampled
importance-weighted gradient and a measure of its effective support.  This
section determines when that gradient is reliable, how its error controls
finite-time policy optimization, and when clipping can reduce that error.  The
argument follows one causal chain: poor coverage raises gradient error, gradient
error enlarges the certified stationary region, and clipping becomes preferable
only when its variance reduction repays its bias.  This chain gives ESS an
operational role rather than treating it as another diagnostic of policy drift.
All proofs are given line by line in Appendix~\ref{app:proofs} so that the main
argument remains visible.

\subsection{When the permissive gradient is reliable}
\label{sec:raw}

The permissive estimator targets the desired on-policy gradient without bias,
but unbiasedness alone does not make a fixed batch reliable.  A small number of
large ratios can dominate the average.  ESS measures this loss of coverage, but
the resulting error also depends on the size of the associated gradient
contributions.  We isolate that second factor through
\begin{equation}
  G_{2,\theta}
  :=\frac{\E_Q[W_\theta^2\|H_\theta\|_2^2]}
          {\E_Q[W_\theta^2]}.
  \label{eq:gradient-scale}
\end{equation}
The quantity $G_{2,\theta}$ is the squared gradient scale under the part of the
rollout distribution emphasized by large ratios.  Separating this scale from
coverage yields the paper's reliability bridge.

\begin{theorem}[ESS reliability bridge]
\label{thm:raw-mse}
Assume $\E_Q[W_\theta^2\|H_\theta\|_2^2]<\infty$.  For fixed $\theta$,
$\widehat g_W$ is unbiased and
\begin{align}
  \E\|\widehat g_W-g\|_2^2
  &=\frac1N\left\{
    \E_Q[W_\theta^2\|H_\theta\|_2^2]-\|g(\theta)\|_2^2
  \right\}
  \label{eq:raw-mse}\\
  &=\frac1N\left(
    \frac{G_{2,\theta}}{\rho_\theta}-\|g(\theta)\|_2^2
  \right)
  \label{eq:ess-factorization}\\
  &\le \frac{G_{2,\theta}}{N\rho_\theta}.
  \label{eq:ess-bridge}
\end{align}
\end{theorem}

The factorization identifies $N\rho_\theta$ as the effective support of the
gradient estimate.  Within a fixed update construction, declining ESS therefore
weakens the reliability bound in direct proportion.  The exact expression keeps
$N$ because it is part of any sample average, but the operational comparison in
this paper holds that construction fixed and uses ESS to locate the reliability
regime.  The next question is whether this statistical error has an optimization
consequence.

\subsection{From gradient MSE to finite-time policy optimization}
\label{sec:mse-improvement}

Smoothness turns gradient reliability into an optimization guarantee.
Theorem~7 of \citet{papini2022smoothing} shows that an $L$-smooth policy
objective satisfies the following lower bound for any feasible parameter step
$d$:
\begin{equation}
  J(\theta+d)-J(\theta)
  \ge g(\theta)^\top d-\frac{L}{2}\|d\|_2^2.
  \label{eq:smooth-policy-improvement}
\end{equation}
The linear term rewards alignment with the true gradient, while the quadratic
term penalizes an inaccurate or excessively large step.  Completing the square
combines both effects into gradient error, so permissive, clipped, and truncated
estimators can be compared on the same optimization scale.

\begin{theorem}[Gradient error controls one-step policy improvement]
\label{thm:mse-improvement}
Fix $\theta$ and let $g=g(\theta)$.  Let $\widehat g$ be any, possibly biased,
gradient estimate.  Suppose $J$ is $L$-smooth on the line segment from
$\theta$ to $\theta+\eta\widehat g$, and that the update is feasible.  For
$0<\eta\le 1/L$,
\begin{equation}
  J(\theta+\eta\widehat g)-J(\theta)
  \ge \frac{\eta}{2}
  \left\{\|g\|_2^2-\|\widehat g-g\|_2^2\right\}.
  \label{eq:pointwise-improvement}
\end{equation}
If $\widehat g$ is random with finite second moment, then
\begin{equation}
  \E\!\left[J(\theta+\eta\widehat g)-J(\theta)\right]
  \ge \frac{\eta}{2}
  \left\{\|g\|_2^2-\E\|\widehat g-g\|_2^2\right\}.
  \label{eq:mse-improvement}
\end{equation}
Consequently, $\E\|\widehat g-g\|_2^2<\|g\|_2^2$ is sufficient for positive
expected policy improvement.
\end{theorem}

The one-step statement extends directly to an entire optimization path.  This
extension matters because policy-gradient convergence is typically assessed
through first-order stationarity, not improvement of a single update.

\begin{corollary}[Finite-time stationarity under gradient error]
\label{cor:finite-time-mse}
Fix $\theta_0$, and let $\theta_{t+1}=\theta_t+\eta\widehat g_t$ for
$t=0,\ldots,T-1$, and let $g_t=\nabla J(\theta_t)$.  Suppose the conditions of
Theorem~\ref{thm:mse-improvement} hold at every update with a common
$0<\eta\le1/L$, and suppose $J(\theta)\le J^\star$ along the path.  Then
\begin{equation}
  \frac1T\sum_{t=0}^{T-1}\|g_t\|_2^2
  \le
  \frac{2\{J^\star-J(\theta_0)\}}{\eta T}
  +\frac1T\sum_{t=0}^{T-1}\|\widehat g_t-g_t\|_2^2.
  \label{eq:finite-time-mse}
\end{equation}
If the estimates are random and $U$ is uniform on
$\{0,\ldots,T-1\}$ independently of the optimization path, then taking
expectation in \eqref{eq:finite-time-mse} gives the corresponding bound on
$\E\|\nabla J(\theta_U)\|_2^2$ in terms of the average gradient MSE.
\end{corollary}

The first term in \eqref{eq:finite-time-mse} vanishes with the optimization
horizon, while the average gradient error determines the certified stationary
region.  Within this guarantee, reducing average gradient MSE by $\delta$
tightens the estimator-dependent term by the same $\delta$, without requiring
the estimator to be unbiased.  This structure agrees with established analyses.
Theorem~11 of
\citet{kallus2020efficient} bounds the average squared policy gradient by an
initial objective gap plus squared off-policy gradient-estimation error.
Corollary~4.6 of \citet{yuan2022general} similarly retains a stochastic
variance term in a conventional policy-gradient guarantee.  Theorem~20 and
Remark~21 of \citet{driggs2022biased} further show that biased-gradient
convergence can depend on bias through MSE, so an estimator may improve the
guarantee when its variance reduction exceeds the cost of its bias.
Corollary~\ref{cor:finite-time-mse} isolates this common principle in the form
needed to compare clipping rules.

The ESS factorization now supplies a coverage-dependent convergence guarantee.
To state it without hiding a dependence assumption, let each gradient batch be
drawn after $\theta_t$ is fixed.

\begin{corollary}[Finite-time ESS certificate under conditional sampling]
\label{cor:ess-convergence}
In the setting of Corollary~\ref{cor:finite-time-mse}, let $\mathcal F_t$
contain the history before update $t$, with $\theta_t$ measurable with respect
to $\mathcal F_t$.  Suppose that, conditional on $\mathcal F_t$,
$\widehat g_t$ is the raw estimator formed from $N$ independent draws from the
rollout distribution $Q_t$.  Let $\rho_t$ and $G_{2,t}$ denote the corresponding
population ESS and weighted gradient scale.  Then
\begin{equation}
  \E\|\nabla J(\theta_U)\|_2^2
  \le
  \frac{2\{J^\star-J(\theta_0)\}}{\eta T}
  +\frac1T\sum_{t=0}^{T-1}
    \E\!\left[\frac{G_{2,t}}{N\rho_t}\right].
  \label{eq:ess-convergence}
\end{equation}
If $G_{2,t}\le\overline G_2$ and $\rho_t\ge\underline\rho>0$ almost surely
along the path, then
\begin{equation}
  \E\|\nabla J(\theta_U)\|_2^2
  \le
  \frac{2\{J^\star-J(\theta_0)\}}{\eta T}
  +\frac{\overline G_2}{N\underline\rho}.
  \label{eq:ess-convergence-uniform}
\end{equation}
\end{corollary}

The result gives effective support a finite-time meaning: at controlled
gradient scale, weak coverage enlarges the certified upper bound on
stationarity error for a permissive policy-gradient path.  For a single update,
the exact MSE identity
also shows that
\begin{equation}
  G_{2,\theta}<(N+1)\rho_\theta\|g(\theta)\|_2^2
  \label{eq:positive-ess-update}
\end{equation}
is sufficient for positive expected improvement.  Neither statement produces
a universal normalized-ESS threshold because the guarantee also depends on
gradient scale and true-gradient magnitude.  Moreover, the ESS substitution in
Corollary~\ref{cor:ess-convergence} requires conditional sampling.  Under
repeated optimization of one fixed rollout, the pathwise bound
\eqref{eq:finite-time-mse} remains valid, but replacing its realized error by
the fixed-$\theta$ ESS identity requires additional adaptive-data control.  The
contextual-bandit study therefore measures this relationship directly along a
reused-rollout path.  Before that comparison, we examine why the remaining
scale term is particularly manageable in RLVR.

\subsection{Why RLVR makes the scale term manageable}

The weighted gradient scale combines two factors: the advantage and the policy
score.  RLVR naturally controls the first factor because verifier rewards are
bounded and group-normalized advantages have finite range.  A score bound or an
empirical scale estimate is still required for the second factor.

\begin{proposition}[A sufficient bound on $G_{2,\theta}$]
\label{prop:g2-bound}
If $|A|\le B_A$ and $\|S_\theta\|_2\le B_S$ almost surely under $Q$, then
\begin{equation}
  G_{2,\theta}\le B_A^2B_S^2.
  \label{eq:g2-bound}
\end{equation}
\end{proposition}

Common RLVR constructions make the advantage factor small.  If the verifier
reward is binary and a prompt-only baseline satisfies $b(X)\in[0,1]$, then
$|A|=|R-b(X)|\le1$.  Outcome-level GRPO instead standardizes rewards within a
group of $K$ responses \citep{shao2024deepseekmath}.  Its standardized
advantages also have a deterministic finite-group bound whenever their sample
variance is nonzero.

\begin{proposition}[Standardized group-advantage bound]
\label{prop:grpo-bound}
Let $d_i=r_i-\bar r$, let $v=K^{-1}\sum_{j=1}^K d_j^2>0$, and let
$A_i=d_i/\sqrt v$.  Then $|A_i|\le\sqrt{K-1}$ for every $i$.
\end{proposition}

These results clarify both the value and the limit of advantage normalization.
It controls one factor in $G_{2,\theta}$, while the score still requires either a
bound or an empirical estimate.  Group normalization also couples the $K$
responses, so Theorem~\ref{thm:raw-mse} cannot be applied response by response
without a group-level sampling argument.  The binary-reward,
prompt-baseline case satisfies the theorem directly; the GRPO result controls
scale but does not supply an independence justification.

The analysis has now linked effective coverage to gradient error and gradient
error to finite-time policy optimization.  This link explains why a permissive
update can work while support remains broad.  It does not yet show that clipping
is the right response when support deteriorates.  That comparison requires the
exact gradient induced by the clipped objective.

\subsection{What clipping changes}
\label{sec:clipping}

Low ESS indicates that an intervention may be useful, but it does not establish
that a particular intervention is better.  PPO clipping can remove unstable
sample contributions, yet it changes the target gradient at the same time.  We
derive that change explicitly because clipping is often confused with globally
truncating the importance ratio.

\subsubsection{PPO clipping as an advantage-sign gradient mask}

For a detached advantage $A$, the per-sample PPO surrogate is
\begin{equation}
  \ell_\epsilon^{\mathrm{PPO}}(\theta)
  :=\min\left\{
    W_\theta A,
    \operatorname{clip}(W_\theta,1-\epsilon,1+\epsilon)A
  \right\}.
  \label{eq:ppo-objective}
\end{equation}
Clipping this scalar objective does not cap the gradient coefficient.  Its
derivative instead masks samples according to advantage sign.

\begin{proposition}[PPO clipping is advantage-sign gradient masking]
\label{prop:ppo-mask}
Away from the two clipping boundaries, define
\begin{equation}
  M_\epsilon
  :=\mathbf{1}\{A\ge0, W_\theta\le1+\epsilon\}
   +\mathbf{1}\{A<0, W_\theta\ge1-\epsilon\}.
  \label{eq:ppo-mask}
\end{equation}
Then
\begin{equation}
  \nabla_\theta\ell_\epsilon^{\mathrm{PPO}}(\theta)
  =W_\theta M_\epsilon H_\theta.
  \label{eq:ppo-gradient}
\end{equation}
\end{proposition}

PPO therefore removes an upper-ratio gradient only for nonnegative advantages
and a lower-ratio gradient only for negative advantages.  In particular, a
large ratio with negative advantage remains active.  PPO clipping is a
directional mask, not a global bound on importance coefficients.

This distinction reveals the relevant trade-off.  The mask is useful only when
the variance it removes is worth the bias it introduces.  To express that
comparison once for PPO and other interventions, let
$C_\theta$ be any scalar coefficient for which $C_\theta H_\theta$ has a finite
second moment.  Define
\begin{equation}
  \widehat g_C:=\frac1N\sum_{i=1}^N C_{\theta,i}H_{\theta,i},
  \qquad
  b_C:=\E_Q[(C_\theta-W_\theta)H_\theta],
  \label{eq:modified-estimator}
\end{equation}
and
\begin{equation}
  \Sigma_W:=\Cov_Q(W_\theta H_\theta),
  \qquad
  \Sigma_C:=\Cov_Q(C_\theta H_\theta).
  \label{eq:bias-covariance}
\end{equation}

\subsubsection{The estimator crossover}

The next result gives the exact risk of the modified coefficient $C_\theta$
relative to the permissive coefficient $W_\theta$.

\begin{theorem}[Exact estimator crossover]
\label{thm:crossover}
For fixed $\theta$ and finite second moments,
\begin{equation}
  \operatorname{MSE}(\widehat g_W)
  =\frac1N\operatorname{tr}(\Sigma_W),
  \qquad
  \operatorname{MSE}(\widehat g_C)
  =\|b_C\|_2^2+\frac1N\operatorname{tr}(\Sigma_C).
  \label{eq:two-risks}
\end{equation}
Therefore,
\begin{equation}
  \operatorname{MSE}(\widehat g_C)
  <\operatorname{MSE}(\widehat g_W)
  \iff
  \operatorname{tr}(\Sigma_W)-\operatorname{tr}(\Sigma_C)
  >N\|b_C\|_2^2.
  \label{eq:crossover}
\end{equation}
\end{theorem}

\begin{corollary}[Bias--variance crossover]
\label{cor:batch-crossover}
Let
\begin{equation}
  \Delta_C
  :=\operatorname{tr}(\Sigma_W)-\operatorname{tr}(\Sigma_C)>0.
  \label{eq:covariance-reduction}
\end{equation}
If $b_C\ne0$, then
\begin{equation}
  \operatorname{MSE}(\widehat g_C)
  <\operatorname{MSE}(\widehat g_W)
  \iff
  N<N_C^*:=\frac{\Delta_C}{\|b_C\|_2^2}.
  \label{eq:batch-crossover}
\end{equation}
Thus the modified estimator is preferred below its critical batch size, while
the unbiased estimator is preferred above it.  If $b_C=0$, the modified
estimator has lower MSE for every finite $N$ whenever $\Delta_C>0$.
\end{corollary}

The crossover condition states the entire reason to clip: the removed
covariance must exceed the squared bias of the modified gradient at the given
update construction.  The corollary records that covariance averages away but
clipping bias does not.  This distinction is retained for theoretical
completeness; the practical rule compares updates under the same construction.
For PPO, the coefficient is
$C_\theta^{\mathrm{PPO}}=W_\theta M_\epsilon$.
Its exact bias is
\begin{align}
  b_\epsilon^{\mathrm{PPO}}
  &=\E_Q[(W_\theta M_\epsilon-W_\theta)H_\theta]
    \label{eq:ppo-bias-1}\\
  &=-\E_Q[W_\theta(1-M_\epsilon)H_\theta].
    \label{eq:ppo-bias-2}
\end{align}
Theorem~\ref{thm:crossover} therefore compares the covariance removed by PPO's
sign-specific mask with the squared bias of the masked gradient.  Combining the theorem with
Theorem~\ref{thm:mse-improvement}, the difference between the modified and raw
MSE-based improvement certificates is
\begin{equation}
  \frac{\eta}{2}\left\{
    \operatorname{MSE}(\widehat g_W)
    -\operatorname{MSE}(\widehat g_C)
  \right\}.
  \label{eq:improvement-crossover}
\end{equation}
Thus Equation~\eqref{eq:crossover} determines which estimator has the stronger
one-step certificate, not merely which estimator has lower gradient risk.

The oracle comparison in Equation~\eqref{eq:crossover} cannot be evaluated
during ordinary training because it requires the true gradient and population
moments.  An ESS gate replaces that unavailable comparison with an observable
event.  The next result states exactly what such a replacement guarantees.

\begin{proposition}[Conditional guarantee for an ESS gate]
\label{prop:ess-gate-guarantee}
Let $D_\tau=\{\widehat\rho<\tau\}$, assume that $D_\tau$ and $D_\tau^c$
have positive probability, and define
\begin{equation}
  \widehat g_\tau
  :=\mathbf{1}_{D_\tau}\widehat g_C
   +\mathbf{1}_{D_\tau^c}\widehat g_W.
  \label{eq:ess-gated-estimator}
\end{equation}
For $j\in\{C,W\}$ and any event $D$, write
$R_j(D):=\E[\|\widehat g_j-g\|_2^2\mid D]$.  Then
\begin{align}
  \operatorname{MSE}(\widehat g_\tau)
  -\operatorname{MSE}(\widehat g_W)
  &=\Pr(D_\tau)\{R_C(D_\tau)-R_W(D_\tau)\},
  \label{eq:gate-versus-raw}\\
  \operatorname{MSE}(\widehat g_\tau)
  -\operatorname{MSE}(\widehat g_C)
  &=\Pr(D_\tau^c)\{R_W(D_\tau^c)-R_C(D_\tau^c)\}.
  \label{eq:gate-versus-clipped}
\end{align}
Hence the gate has no larger MSE than either static estimator whenever
$R_C(D_\tau)\le R_W(D_\tau)$ and
$R_W(D_\tau^c)\le R_C(D_\tau^c)$.  Under the assumptions of
Theorem~\ref{thm:mse-improvement}, the same ordering holds for the associated
MSE-based policy-improvement certificates.
\end{proposition}

\begin{proof}
Because the two events partition the sample space and the gate equals one
estimator on each event,
\begin{align}
  \operatorname{MSE}(\widehat g_\tau)
  &=\E[\|\widehat g_\tau-g\|_2^2\mathbf{1}_{D_\tau}]
    +\E[\|\widehat g_\tau-g\|_2^2\mathbf{1}_{D_\tau^c}]\\
  &=\Pr(D_\tau)R_C(D_\tau)
    +\Pr(D_\tau^c)R_W(D_\tau^c).
  \label{eq:gate-mse-decomposition}
\end{align}
Similarly,
\begin{align}
  \operatorname{MSE}(\widehat g_W)
  &=\Pr(D_\tau)R_W(D_\tau)
    +\Pr(D_\tau^c)R_W(D_\tau^c),\\
  \operatorname{MSE}(\widehat g_C)
  &=\Pr(D_\tau)R_C(D_\tau)
    +\Pr(D_\tau^c)R_C(D_\tau^c).
\end{align}
Subtracting each static risk from the gated risk gives
Equations~\eqref{eq:gate-versus-raw} and
\eqref{eq:gate-versus-clipped}.  The risk ordering follows term by term.
The final claim follows by substituting each MSE into
Equation~\eqref{eq:mse-improvement} at the same learning rate.
\end{proof}

Proposition~\ref{prop:ess-gate-guarantee} does not make $0.1$ a universal
constant.  It instead gives a falsifiable requirement for any proposed
threshold: below the threshold, masking should reduce conditional gradient
MSE; above it, the permissive estimator should retain the advantage.  The
contextual-bandit experiment will first verify the oracle crossover and then
test whether the observable choice $\tau=0.1$ satisfies the low-ESS part of
this requirement well enough to serve as a practical bridge to RLVR.
 
\subsubsection{Ratio truncation bias trade-off}

Ratio truncation changes the importance coefficient after the gradient has been
specified, so it is distinct from the derivative of the PPO objective.  For
$c>0$, define
\begin{equation}
  C_c^{\mathrm{tr}}:=\min(W_\theta,c),
  \qquad
  T_c:=(W_\theta-c)_+,
  \qquad
  \tau_c:=\E_Q[T_c].
  \label{eq:upper-truncation}
\end{equation}
The contribution above $c$ is then $cH_\theta$, not zero.  This is a detached
importance-weight estimator, not the derivative of the PPO surrogate.

\begin{corollary}[Bias and MSE of upper-ratio truncation]
\label{cor:upper-truncation}
Suppose $\|H_\theta\|_2\le H_{\max}$ almost surely.  The detached estimator
$\widehat g_c=N^{-1}\sum_i C_{c,i}^{\mathrm{tr}}H_{\theta,i}$ satisfies
\begin{align}
  b_c&=-\E_Q[T_cH_\theta],
  &\|b_c\|_2&\le H_{\max}\tau_c,
  \label{eq:cap-bias}\\
  \operatorname{MSE}(\widehat g_c)
  &\le H_{\max}^2\left(\tau_c^2+\frac{c}{N}\right).
  \label{eq:cap-mse-bound}
\end{align}
\end{corollary}

The result isolates the appeal of upper truncation.  It replaces the raw
second-moment term by $c/N$, while the introduced bias remains small when the
tail excess $\tau_c$ is small.  The cap is therefore a tunable estimator
parameter, not part of the paper's central claim.  Truncated importance ratios
also appear in ACER with
bias correction \citep{wang2017acer} and in the V-trace correction used by
IMPALA \citep{espeholt2018impala}; recent LLM methods likewise control extreme
weight moments \citep{zheng2025m2po,fakoor2026p3o}.  These precedents motivate
truncation as a separate importance-sampling device.  They do not make it
equivalent to PPO clipping.

The theory now yields a complete decision logic.  When $G_{2,\theta}$ is
controlled, effective support governs permissive-gradient reliability and its
policy-improvement certificate.  Clipping becomes preferable only when its
covariance reduction exceeds the bias penalty.  Thus, within a fixed training
configuration, ESS can locate the crossover between permissive and safeguarded
updates.  The final step is to turn this population comparison into an
observable training rule.

\subsection{The ESS-guided decision rule}
\label{sec:practice}

Population ESS is unavailable during training, but its plug-in estimate is
\begin{equation}
  \widehat\rho
  :=\frac{(\sum_{i=1}^N W_i)^2}{N\sum_{i=1}^N W_i^2}.
  \label{eq:sample-ess}
\end{equation}
The statistic is computed from raw sequence ratios before clipping or
truncation.  We keep the update construction and number of valid sequences
fixed within each reported comparison, so $\widehat\rho$ directly orders the
reliability of the competing updates.  Appendix~\ref{app:bandit} reports the
sensitivity to estimator construction.

The theory motivates a reliability gate but does not determine a numerical
threshold.  The exact boundary also depends on clipping bias, gradient scale,
and signal, while sample ESS can miss unobserved tail mass.  Moreover, the same
batch usually determines both the gate and the gradient, and a token-local LLM
surrogate is not algebraically identical to
Equation~\eqref{eq:raw-estimator}.  We therefore treat $\widehat\rho=0.1$ as a
prespecified application heuristic, not as a theoretical constant.  Recent LLM
RL methods likewise adapt updates using ESS, weight moments, or sequence
reshaping \cite{zheng2025m2po,shen2026vespo,huang2026vcpo,fakoor2026p3o}.

Section~\ref{sec:simulation} tests whether these theoretical quantities support
an actual update decision.  A contextual bandit exposes the population gradient
needed to validate the MSE analysis, while an RLVR-style optimization loop tests
whether the resulting ESS gate improves policy learning.  This controlled bridge
must succeed before the rule is transferred to language-model training.

\section{Contextual-bandit validation of the ESS decision rule}
\label{sec:simulation}

The purpose of this study is to validate the paper's decision logic, not to
benchmark contextual-bandit performance.  It asks three questions in the order
required by the theory.  First, does ESS predict the error and policy consequence
of a permissive gradient?  Second, does ESS locate the raw-versus-PPO MSE
crossover under a fixed diagnostic setting?  Third, does the observable $0.1$
gate select high-risk updates and improve policy optimization?  Answering all
three turns ESS from a descriptive statistic into a candidate training rule.

\subsection{Why a contextual-bandit testbed?}

An LLM experiment cannot answer the first two questions directly because its
population gradient and reward are unknown.  We therefore use the standard
classification-to-contextual-bandit reduction
\citep{wang2017adaptive}.  An Optdigits image is the context, the policy action
is a 16-token binary encoding of its digit, and an exact-match verifier returns
one only for the correct sequence \citep{alpaydin1998optdigits}.  The finite
population makes the policy gradient and reward exact, while the sequence action
retains the product importance ratio that drives ESS in RLVR.  The testbed is
therefore deliberately simpler than language generation but better suited to
testing the theory.

We embed this bandit in the data flow used by RLVR
\citep{shao2024deepseekmath,yu2025dapo}.  Each rollout generates grouped
responses, computes leave-one-out advantages, and supplies 16 sequential policy
minibatches.  Ratios and sample ESS are recomputed before every update, and a new
rollout is collected only after the current one has been optimized.  ESS thus
changes because the current policy moves away from its rollout policy, not
because we impose an artificial ratio distribution.  We compare raw, PPO-masked,
and ESS-gated updates in 100 paired replications.  Appendix~\ref{app:bandit}
specifies the task, oracle, randomization, and aggregation protocol in full.

\subsection{Oracle measurements verify the theory}

We begin with the quantities that are observable only because the bandit has
an exact oracle.  The first test evaluates the permissive estimator against the
population gradient and then evaluates the resulting policy step against the
population reward.  Its gradient MSE is $1.6\times10^{-2}$ above ESS $0.8$,
rises to $1.31\times10^{-1}$ between $0.03$ and $0.1$, and reaches
$4.77\times10^{-1}$ below $0.03$.  The optimization consequence appears in the
same region.  Permissive steps reduce reward by $4.0\%$ in the
$[0.03,0.1)$ interval and by $13.3\%$ below $0.03$, whereas the corresponding
oracle-gradient steps improve reward in every interval.  ESS therefore
identifies estimation failure rather than an unfavorable population objective.
Appendix Figure~\ref{fig:ess-validation} and
Table~\ref{tab:rlvr-diagnostics} report the complete result.

The second test evaluates the estimator comparison in
Theorem~\ref{thm:crossover}.  At 56 frozen policy states under the fixed oracle
diagnostic, PPO masking has lower MSE at low ESS and the permissive estimator
has lower MSE at high ESS.  The preference changes between the $[0.2,0.4)$ and
$[0.4,0.6)$ intervals.  This is the predicted bias--variance crossover:
clipping is preferred only where its covariance reduction exceeds its bias
penalty.  Appendix Figure~\ref{fig:ess-crossover} and
Table~\ref{tab:rlvr-crossover} report the paired MSE estimates and the full
sensitivity analysis.

\begin{figure}[htbp]
  \centering
  \includegraphics[width=\linewidth]{figures/ess_formula_oracle.pdf}
  \caption{From oracle verification to an observable ESS rule.
  (a) Under the fixed diagnostic configuration, the predicted MSE preference
  changes sign with population ESS.  (b) Along the fixed-$0.1$ trajectory, the
  bars show how often the
  shadow PPO gradient has lower MSE than the shadow raw gradient in each
  sample-ESS region.  The orange bar is the event selected by the gate; blue
  bars provide nearby context.  Labels give updates per run and error bars are
  standard errors across 100 replications.  (c) Mean relative reward improvement in a
  four-epoch optimization loop; bands are 95\% confidence intervals over the
  same paired runs.}
  \label{fig:formula-oracle}
\end{figure}

We next ask whether the oracle crossover can be identified without using its
evaluation outcomes.  Within each of the seven ESS intervals, four frozen
states fit the decision boundary and four remain for evaluation.  Calibration
estimates the bias and covariance terms in Theorem~\ref{thm:crossover} and fixes
an interior reference configuration before the held-out redraws are examined.
The resulting ESS boundary is $0.42$.  On 100 independent redraws of each
held-out state, the rule reduces MSE by $4.3\%\pm1.1\%$ relative to the better
static estimator.  Thus ESS locates the raw-versus-PPO crossover without using
test outcomes.  Appendix~\ref{app:bandit} gives the complete selection protocol
and sensitivity to alternative estimator constructions.

\subsection{A sparse 0.1 heuristic bridges the oracle and RLVR}

The fitted boundary above is not directly available in ordinary RLVR because
the true gradient, clipping bias, and population covariance cannot be computed.
Sample ESS is available before every update.  We therefore use
$\widehat\rho<0.1$ as a simple trigger, not as an estimate of the oracle
boundary.  Proposition~\ref{prop:ess-gate-guarantee} gives the relevant
one-sided guarantee: if PPO has lower conditional MSE on the selected
low-ESS event, replacing raw by PPO only on that event lowers the gate's MSE
relative to always using raw and strengthens its MSE-based improvement
certificate.  The bandit lets us test that premise before carrying the rule to
language models.

Panel (b) performs this test along the actual fixed-$0.1$ optimization
trajectory.  At every update below the oracle reference boundary, we compute
shadow raw and PPO gradients against the exact population gradient without
altering training.  Below $0.1$, PPO has lower MSE on
$83.7\%\pm1.4\%$ of updates and reduces conditional MSE by
$53.3\%\pm2.9\%$.  The gate selects only $3.3\%$ of optimizer steps, so
$0.1$ identifies a sparse, high-confidence subset of the failures predicted by
the oracle analysis.  It is intentionally not a complete reconstruction of the
$0.42$ testbed-specific crossover.

Panel (c) then asks whether this selective intervention helps repeated policy
optimization.  Across the four-epoch study and 100 paired runs, the fixed-$0.1$
gate reaches final reward
$0.4940\pm0.0050$, compared with $0.4606\pm0.0098$ for raw and
$0.4038\pm0.0005$ for always applying PPO masking.  It matches the raw
median endpoint to four decimals while increasing the tenth-percentile endpoint
from $0.352$ to $0.495$.  The calibrated $0.42$ gate has the slightly higher
mean, $0.4961\pm0.0003$, because its 61.7 masked updates remove a small number
of severe failures.  The sparse $0.1$ gate nevertheless finishes above it in
90 of 100 paired runs while masking only 16.9 updates.  Thus $0.1$ is a useful
efficiency-oriented heuristic, whereas the fitted boundary is the stronger
tail-risk safeguard.

This separation supplies the bridge to the main experiment.  The oracle study
validates the theoretical crossover.  The on-trajectory diagnostic then
verifies the conditional MSE premise needed for the observable $0.1$ trigger,
and repeated optimization shows what that sparse intervention buys.  The next
section tests the same prespecified trigger in RLVR, where only its observable
inputs and training consequences remain.

\section{Main experiments: ESS-gated RLVR}
\label{sec:llm-experiments}

The contextual-bandit study establishes the mechanism under an exact oracle.
The main experiment tests the prespecified $0.1$ application gate in
language-model training, where neither the population gradient nor its MSE is
observable.  Evidence must therefore come from matched training trajectories:
the gate should preserve the early efficiency of a permissive update while
improving its late stability.

\subsection{Decision rule and comparison set}

At every policy update, we compute sequence ESS from the raw ratios before any
masking or truncation.  All methods use the same valid sequences and minibatch
construction.  We then evaluate $0.1$ as a prespecified heuristic:
when $\widehat\rho\ge0.1$, the method retains the available learning signal
using either the fully permissive estimator or the CISPO-style weight-capped
estimator \citep{minimax2025m1}; when $\widehat\rho<0.1$, it activates one of
three safeguards: conventional PPO clipping, Clip-Higher
\citep{yu2025dapo}, or the divergence constraint of DPPO
\citep{qi2026dppo}.

\begin{equation}
  \widehat g_{\mathrm{LLM}}
  =\begin{cases}
    \widehat g_{\mathrm{permissive}},&\widehat\rho\ge0.1,\\
    \widehat g_{\mathrm{safeguard}},&\widehat\rho<0.1.
  \end{cases}
  \label{eq:llm-gate}
\end{equation}
Each gated method is compared with its always-permissive and always-protected
counterparts under identical rollout, optimizer, token-budget, and evaluation
settings.  This matching separates the value of the ESS decision from the value
of the safeguard itself.  The threshold is fixed before the reported runs and
is not retuned by safeguard, model, or benchmark.  It is not claimed to be the
theoretical crossover.  The number of valid sequences is reported and held
fixed within every matched comparison.

\subsection{Evaluation protocol}

The primary result will report evaluation reward against training steps for the
permissive baseline, each always-active safeguard, and its ESS-gated version.
A companion panel will align sequence ESS to the same horizontal axis and mark
every switch in Equation~\eqref{eq:llm-gate}.  For at least three seeds, we will
report the mean and confidence interval, peak and final reward, tokens to a
fixed reward target, and a prespecified measure of post-peak collapse.  Entropy
and response length will serve as diagnostics for the motivating failure mode.

\begin{figure}[H]
  \centering
  \fbox{\parbox[c][1.42in][c]{0.94\linewidth}{\centering
  \textbf{Main RLVR result placeholder}\\[4pt]
  Insert matched reward curves for: permissive update, always PPO, ESS-gated
  PPO, always Clip-Higher, ESS-gated Clip-Higher, always DPPO, and ESS-gated
  DPPO.  Show mean and confidence interval across seeds.}}
  \caption{Main RLVR comparison placeholder.  Each safeguard is evaluated when
  always active and when activated by the prespecified ESS-$0.1$ heuristic,
  under the same rollout and evaluation protocol.}
  \label{fig:llm-main}
\end{figure}

\begin{table}[H]
\centering
\small
\caption{Main RLVR results to be completed from the matched runs.  Higher is
better for peak and final reward; lower is better for time to target and
post-peak collapse.}
\label{tab:llm-main}
\begin{tabular}{lrrrr}
\toprule
Update rule & Peak reward $\uparrow$ & Final reward $\uparrow$ & Time to target $\downarrow$ & Collapse $\downarrow$ \\
\midrule
Permissive only & TBD & TBD & TBD & TBD \\
PPO always active & TBD & TBD & TBD & TBD \\
ESS-gated PPO & TBD & TBD & TBD & TBD \\
Clip-Higher always active & TBD & TBD & TBD & TBD \\
ESS-gated Clip-Higher & TBD & TBD & TBD & TBD \\
DPPO always active & TBD & TBD & TBD & TBD \\
ESS-gated DPPO & TBD & TBD & TBD & TBD \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Interpretation criterion}

The transfer claim is deliberately narrow.  If each gated variant retains the
early progress of the permissive estimator while improving final reward or
reducing collapse relative to its static alternatives, then ESS identifies a
useful regime change across distinct safeguards.  This paragraph remains a
placeholder until the measured effect sizes and uncertainty are available; at
present, the contextual-bandit study is the paper's completed empirical evidence.

\appendix

\section{Contextual-bandit protocol and additional results}
\label{app:bandit}

This appendix defines every quantity used in the contextual-bandit study.  We
first map the finite verifier task to an RLVR update, then derive the population
gradient, specify the rollout and minibatch order, and finally report the full
diagnostic and optimization results supporting Figures~\ref{fig:ess-validation}
and \ref{fig:formula-oracle}.

\subsection{Task and correspondence to RLVR}

The testbed turns supervised classification into a verifier task using the
standard contextual-bandit reduction of \citet{wang2017adaptive}: an input is
presented as the context, a policy samples an answer, and reward is one only for
the correct label.  We replace the single label action with a token sequence and
embed this task in the grouped rollout and minibatch structure used by RLVR
\citep{shao2024deepseekmath,yu2025dapo}.  Table
\ref{tab:rlvr-correspondence} records the resulting correspondence.

\begin{table}[htbp]
\centering
\small
\caption{Correspondence between RLVR and the contextual-bandit verifier task.}
\label{tab:rlvr-correspondence}
\begin{tabular}{lll}
\toprule
RLVR object & Bandit object & Realization \\
\midrule
Prompt $X$ & Context $x$ & Optdigits feature vector \\
Complete response $Y$ & Sequence $Y_{1:16}$ & 16-token binary digit code \\
Verifier reward $R$ & Exact-match reward & $\mathbf{1}\{Y_{1:16}=c(y)\}$ \\
Rollout policy $Q$ & Old policy & Policy that generated the responses \\
Current policy $P_\theta$ & Updated policy & Policy used for the minibatch update \\
Sequence ratio & Importance ratio & $P_\theta(Y_{1:16}\mid x)/Q(Y_{1:16}\mid x)$ \\
Rollout batch & Grouped responses & 128 prompts, 16 responses per prompt \\
PPO epoch & Minibatch sequence & 16 sequential updates, then a new rollout \\
\bottomrule
\end{tabular}
\end{table}

The testbed uses a sequence policy, not a language model.  Its token heads are
conditionally independent so that the population gradient remains exact, but
its response ratio is the same product of token ratios that appears in RLVR.
During training, the learner observes only sampled responses, rollout log
probabilities, and verifier rewards.  Labels are used only by the oracle that
evaluates the finite-population reward and gradient.  This separation preserves
the RLVR feedback structure while exposing the quantities needed to test the
theory.

\subsection{Finite population and initial policy}

Optdigits is a handwritten-digit classification benchmark with 5,620 labeled
examples
\citep{alpaydin1998optdigits}.  Each original $32\times32$ binary image is
partitioned into $4\times4$ blocks; foreground counts produce 64 integer
features in $\{0,\ldots,16\}$.  We concatenate the supplied 3,823 training and
1,797 test examples, divide each feature by 16, and append an intercept.  This
gives a finite population $\{(x_j,y_j)\}_{j=1}^{M}$ with $M=5{,}620$ and
$x_j\in\mathbb{R}^{65}$.  The original split is not used because the estimand
is finite-population policy optimization, not classification generalization.

Each label $y\in\{0,\ldots,9\}$ has a four-bit binary representation.  Let
$c(y)\in\{0,1\}^{16}$ repeat this code four times.  The repetition changes only
the response length, not which answer is correct, and creates a sequence ratio
that compounds over 16 token probabilities.  The policy has one Bernoulli head
per token position,
\begin{align}
  p_{\theta,\ell}(x)&=\sigma(\theta_\ell^\top x),\nonumber\\
  P_\theta(a_{1:16}\mid x)
  &=\prod_{\ell=1}^{16}
    p_{\theta,\ell}(x)^{a_\ell}
    \{1-p_{\theta,\ell}(x)\}^{1-a_\ell},
  \qquad \theta\in\mathbb{R}^{16\times65}.
  \label{eq:optdigits-policy}
\end{align}
The verifier reward is
$R(x_j,a_{1:16})=\mathbf{1}\{a_{1:16}=c(y_j)\}$.

Initialization is deterministic and uses no experimental outcomes.  Starting
from zero, we fit all token heads for 500 full-population logistic-regression
steps with step size $0.8$ and no regularization.  We then multiply the fitted
weights by $0.55$ to keep the initial policy stochastic, which gives exact
initial reward $V(\theta_0)=0.095766$.  This procedure uses no data split,
minibatching, early stopping, or model selection.

For compactness, define
\begin{equation}
  q_j(\theta)
  :=P_\theta(c(y_j)\mid x_j)
  =\prod_{\ell=1}^{16}
  \left[c_{j\ell}p_{j\ell}
  +(1-c_{j\ell})(1-p_{j\ell})\right],
  \label{eq:correct-sequence-probability}
\end{equation}
where $c_{j\ell}=c(y_j)_\ell$ and
$p_{j\ell}=p_{\theta,\ell}(x_j)$.  The finite-population objective is
\begin{equation}
  V(\theta)=\frac1M\sum_{j=1}^{M}q_j(\theta).
  \label{eq:optdigits-value}
\end{equation}

\subsection{Exact gradient and group-relative advantage}

The finite population makes the target gradient available in closed form.  For
token position $\ell$, define the probability assigned to its correct bit as
$h_{j\ell}=c_{j\ell}p_{j\ell}+(1-c_{j\ell})(1-p_{j\ell})$.  The Bernoulli score
identity gives
\begin{align}
  \nabla_{\theta_\ell}q_j(\theta)
  &=q_j(\theta)\nabla_{\theta_\ell}\log q_j(\theta)
    \label{eq:optdigits-gradient-proof-1}\\
  &=q_j(\theta)\nabla_{\theta_\ell}\log h_{j\ell}
    \label{eq:optdigits-gradient-proof-2}\\
  &=q_j(\theta)\{c_{j\ell}-p_{j\ell}\}x_j.
    \label{eq:optdigits-gradient-proof-3}
\end{align}
The second line holds because $\theta_\ell$ appears only in token head $\ell$.
For $c_{j\ell}=1$, the last derivative is
$\nabla\log p_{j\ell}=(1-p_{j\ell})x_j$; for $c_{j\ell}=0$, it is
$\nabla\log(1-p_{j\ell})=-p_{j\ell}x_j$.  Both cases equal
$(c_{j\ell}-p_{j\ell})x_j$.  Averaging
Equation~\eqref{eq:optdigits-gradient-proof-3} over all prompts yields
\begin{equation}
  \nabla_{\theta_\ell}V(\theta)
  =\frac1M\sum_{j=1}^{M}
    q_j(\theta)\{c_{j\ell}-p_{j\ell}\}x_j.
  \label{eq:optdigits-gradient}
\end{equation}
Thus the oracle is a deterministic sum over all 5,620 prompts at the current
policy parameters, not a Monte Carlo estimate from a larger rollout.  For every
recorded minibatch update, we compare the sampled gradient and this oracle at
the same pre-update parameter value.  Their squared Frobenius error is
\begin{equation}
  E_t=\|\widehat g_t-\nabla V(\theta_t)\|_F^2,
  \label{eq:optdigits-gradient-error}
\end{equation}
which sums squared coordinate error across all $16\times65=1{,}040$ policy
parameters.  This quantity is the gradient MSE reported in the figure after
averaging $E_t$ within each fixed ESS bin.

For prompt $i$, let $Y_{ig}$ be complete response
$g\in\{1,\ldots,G\}$ and let $R_{ig}=R(x_i,Y_{ig})$ be its verifier reward.  We
use the leave-one-out group advantage
\begin{equation}
  A_{ig}=R_{ig}-\frac1{G-1}\sum_{h\ne g}R_{ih}
  =\frac{G}{G-1}(R_{ig}-\bar R_i).
  \label{eq:loo-group-advantage}
\end{equation}
It has the same sign as the group-centered advantage used in GRPO
\citep{shao2024deepseekmath}.  We omit group standard-deviation scaling so that
all minibatches target the same population gradient.  To verify that the
leave-one-out baseline preserves this target, let
$B_{i,-g}=(G-1)^{-1}\sum_{h\ne g}R_{ih}$ and define
\begin{equation}
  W_{ig}=\frac{P_\theta(Y_{ig}\mid x_i)}{Q(Y_{ig}\mid x_i)},
  \qquad
  S_{ig}=\nabla_\theta\log P_\theta(Y_{ig}\mid x_i).
  \label{eq:loo-weight-score}
\end{equation}
Conditional on the prompt, $B_{i,-g}$ is independent of the focal response.
Therefore,
\begin{align}
  &\E_Q[W_{ig}A_{ig}S_{ig}\mid x_i]
    \label{eq:loo-unbiased-proof-1}\\
  &\quad=\E_Q[W_{ig}R_{ig}S_{ig}\mid x_i]
  -\E_Q[B_{i,-g}\mid x_i]\E_Q[W_{ig}S_{ig}\mid x_i]
    \label{eq:loo-unbiased-proof-2}\\
  &\quad=\E_{P_\theta}[R_{ig}S_{ig}\mid x_i]
  -\E_Q[B_{i,-g}\mid x_i]\E_{P_\theta}[S_{ig}\mid x_i]
    \label{eq:loo-unbiased-proof-3}\\
  &\quad=\nabla_\theta\E_{P_\theta}[R_{ig}\mid x_i].
  \label{eq:loo-unbiased-proof}
\end{align}
Equation~\eqref{eq:loo-unbiased-proof-2} uses independence of the focal response
and leave-one-out baseline.  Equation~\eqref{eq:loo-unbiased-proof-3} changes
measure from $Q$ to $P_\theta$.  The final line uses the zero-mean score identity
for the baseline term and the score-function identity for the reward term.  If
every response in a group has the same reward, all group advantages are zero.

\subsection{Rollout and minibatch procedure}

Each replication begins from the same $\theta_0$ and processes eight newly
generated rollout batches.  For rollout $r$, the testbed executes the
following operations in order.
\begin{enumerate}
  \item Set the rollout policy $Q_r=P_{\theta_{r,0}}$ and sample 128 distinct
  prompts uniformly from the finite population.
  \item For each prompt, sample $G=16$ complete responses from $Q_r$, compute
  the binary verifier rewards, and compute Equation
  \eqref{eq:loo-group-advantage} once.
  \item Randomly permute the 128 prompt groups and partition them into 16
  minibatches.  Each minibatch contains eight intact groups, hence $N=128$
  complete responses.
  \item For minibatch $t=1,\ldots,16$, compute the current sequence ratio
  \begin{equation}
    W_{ig,t}
    =\frac{P_{\theta_{r,t-1}}(Y_{ig}\mid x_i)}
    {Q_r(Y_{ig}\mid x_i)}
    =\prod_{\ell=1}^{16}
    \frac{P_{\theta_{r,t-1}}(Y_{ig\ell}\mid x_i)}
    {Q_r(Y_{ig\ell}\mid x_i)},
    \label{eq:simulation-sequence-ratio}
  \end{equation}
  then compute raw ESS, choose the method-specific gradient, and update
  $\theta_{r,t}=\theta_{r,t-1}+3\widehat g_{r,t}$.
  \item After the sixteenth minibatch, set
  $\theta_{r+1,0}=\theta_{r,16}$ and generate a new rollout batch from that
  updated policy.
\end{enumerate}
Every sampled response enters exactly one update.  Within a rollout, verifier
rewards, advantages, and rollout log probabilities remain fixed.  Current
probabilities, ratios, scores, ESS, and the gate decision are recomputed before
each minibatch update.  This order matches grouped RLVR: generate responses from
the old policy, optimize sequential minibatches, and only then collect a new
rollout from the updated policy.

Sixteen minibatches are needed to expose the failure regime rather than to
increase the apparent gain.  Across the diagnostic trajectories, no update in
positions 1 through 4 has ESS below $0.1$.  The low-ESS frequency is at most 5\%
in positions 5 through 8 and reaches 10\% at position 16.  A four- or
eight-minibatch design would therefore make the gate nearly identical to the
permissive method and would not test the regime it is designed to detect.

\subsection{Estimators, gate, and diagnostics}

Let $S_{ig,t}=\nabla_\theta\log
P_{\theta_{r,t-1}}(Y_{ig}\mid x_i)$.  Each minibatch contains eight prompt groups
and 16 responses per group, so $N=128$.  Summing over these 128 complete
responses, the permissive and PPO-masked gradients are
\begin{align}
  \widehat g_{\mathrm{raw},t}
  &=\frac1N\sum_{i,g}W_{ig,t}A_{ig}S_{ig,t},
    \label{eq:simulation-raw-gradient}\\
  \widehat g_{\mathrm{PPO},t}
  &=\frac1N\sum_{i,g}W_{ig,t}M_{ig,t}A_{ig}S_{ig,t},
    \label{eq:simulation-ppo-gradient}
\end{align}
where $M_{ig,t}$ is the advantage-sign mask in
Proposition~\ref{prop:ppo-mask} with $\epsilon=0.2$.  This is gradient masking,
not numerical ratio truncation.  The third method uses
\begin{equation}
  \widehat g_{\mathrm{gate},t}
  =\begin{cases}
    \widehat g_{\mathrm{raw},t},&\widehat\rho_t\ge0.1,\\
    \widehat g_{\mathrm{PPO},t},&\widehat\rho_t<0.1,
  \end{cases}
  \qquad
  \widehat\rho_t
  =\frac{(\sum_{i,g}W_{ig,t})^2}
  {N\sum_{i,g}W_{ig,t}^2}.
  \label{eq:simulation-gate}
\end{equation}
The threshold is fixed before the runs and is not selected from any reported
outcome.  It is an operational heuristic for this $N=128$ update, not the
theoretical crossover threshold.

All three methods use identical initial weights and are paired on prompt draws,
response-sampling uniforms, and group permutations.  Their policies diverge
after the first update, but applying a shared uniform draw to each method's own
token probability remains a valid coupling and reduces comparison noise.  The
master seed is 73.  We run 100 independent replications, each with
$8\times16=128$ updates.

Panels (a) and (b) use every minibatch from the first 20 ESS-gated
replications, yielding $20\times8\times16=2{,}560$ diagnostic observations.  At
each pre-update policy, we compute Equation~\eqref{eq:optdigits-gradient} and
record $\|\widehat g_{\mathrm{raw},t}-\nabla V(\theta_t)\|_F^2$.  We then evaluate
two counterfactual one-step policies, one updated by the sampled permissive
gradient and the other by the oracle gradient, without changing the actual
training trajectory.  Their outcomes isolate whether a harmful sampled step is
caused by estimation error or by an unfavorable population gradient.

Before running the experiment, we fixed seven ESS intervals:
\[
  \begin{gathered}
    \relax[0,0.03),\ [0.03,0.1),\ [0.1,0.2),\ [0.2,0.4),\\
    [0.4,0.6),\ [0.6,0.8),\ [0.8,1].
  \end{gathered}
\]
Panels (a) and (b) report the within-bin mean and its standard error.
Panel (c) uses all 100 replications and reports
\begin{equation}
  \Delta_r^{\mathrm{rel}}
  =100\frac{V(\theta_{r,0})-V(\theta_{0,0})}
  {V(\theta_{0,0})}.
  \label{eq:relative-improvement}
\end{equation}
after each rollout.  The plotted line is the replication mean and the band is
the mean plus or minus $1.96$ standard errors.  Response counts include every
complete response sent to the verifier; one rollout therefore costs
$128\times16=2{,}048$ verifier responses.  A method reaches the doubling target
at the first post-rollout checkpoint for which its mean relative improvement
exceeds 100\%.

\subsection{Fixed-checkpoint crossover diagnostic}

The trajectory diagnostic above averages one realized squared error over
changing policy states.  To estimate conditional MSE directly, we freeze both
the rollout policy and the current policy at selected pre-update checkpoints
and resample independent minibatches from the frozen rollout policy.  This
separate diagnostic changes neither the selected gradient nor the training
trajectory.

The finite Bernoulli policy also makes population sequence ESS exact.  For
prompt $x$ and token position $\ell$, let $q_{x\ell}$ and $p_{x\ell}$ denote
the rollout and current probabilities of token one.  Independence across the
16 response positions gives
\begin{align}
  \E_Q[W^2\mid x]
  &=\prod_{\ell=1}^{16}
    \left\{
      \frac{p_{x\ell}^2}{q_{x\ell}}
      +\frac{(1-p_{x\ell})^2}{1-q_{x\ell}}
    \right\},
    \label{eq:simulation-weight-second-moment}\\
  \rho_{\mathrm{pop}}
  &=\left\{
    \frac1{5620}\sum_{x=1}^{5620}\E_Q[W^2\mid x]
  \right\}^{-1}.
    \label{eq:simulation-population-ess}
\end{align}
The identity uses $\E_Q[W]=1$.  Unlike the sample statistic in
Equation~\eqref{eq:simulation-gate}, Equation~\eqref{eq:simulation-population-ess}
contains no minibatch noise.

We compute $\rho_{\mathrm{pop}}$ at all 2,560 checkpoints from the first 20
ESS-gated replications.  Using the seven intervals fixed above, we sample eight
checkpoints without replacement from each interval, for 56 frozen states in
total.  Checkpoint selection uses seed 99,246.  At each state, a fresh base
minibatch draws eight prompts without replacement from the 5,620-image
population and 16 responses independently from the frozen rollout policy for
each prompt.  Group rewards and leave-one-out advantages are recomputed from
these responses.  The diagnostic random stream uses seed 271,901.

For each frozen state, we construct 32 paired raw and PPO-masked estimates at
two sizes.  The $N=128$ estimate uses one base minibatch.  The $N=512$ estimate
averages four independent base-minibatch gradients; prompts may therefore
repeat across the four base minibatches.  The first base minibatch is shared by
the two sizes, and raw and PPO estimators always use exactly the same prompts
and responses.  Because responses within one prompt share a leave-one-out
advantage, they are not independent contributions.  The independent unit for
the batch-size comparison is therefore the complete base-minibatch block.  The
move from $N=128$ to $N=512$ averages four independent blocks, dividing the
block-level covariance by four while leaving estimator bias unchanged.  We
retain $N$ as the number of sequences because that is the optimizer convention,
but interpret Corollary~\ref{cor:batch-crossover} at this block level.  For
checkpoint $c$, estimator $e$, and size $N$, we report
\begin{equation}
  \widehat{\operatorname{MSE}}_{c,e,N}
  =\frac1{32}\sum_{s=1}^{32}
    \left\|
      \widehat g_{c,e,N}^{(s)}-\nabla V(\theta_c)
    \right\|_F^2.
  \label{eq:simulation-crossover-mse}
\end{equation}
The exact gradient is recomputed once at each frozen current policy.  We first
average redraws within a checkpoint, then average the eight checkpoint MSEs in
each population-ESS interval.  Standard errors are computed across the eight
checkpoints, not across the 32 redraws.  Panel (b) of
Figure~\ref{fig:ess-crossover} reports the checkpoint mean of
$100(\widehat{\operatorname{MSE}}_{\mathrm{raw}}-
\widehat{\operatorname{MSE}}_{\mathrm{PPO}})/
\widehat{\operatorname{MSE}}_{\mathrm{raw}}$; positive values favor PPO
masking.  The released redraw file also records both counterfactual one-step
reward changes, although Figure~\ref{fig:ess-crossover} focuses on the gradient
risk required by Theorem~\ref{thm:crossover}.

\begin{figure}[htbp]
  \centering
  \includegraphics[width=\linewidth]{figures/ess_estimator_crossover.pdf}
  \caption{Direct raw-versus-PPO gradient-MSE diagnostic at frozen policy
  states.  (a) At $N=512$, PPO masking has lower MSE in the low-ESS regime,
  whereas the raw estimator has lower MSE at high ESS.  (b) PPO MSE reduction
  relative to the raw estimator for $N=128$ and $N=512$; positive values favor
  PPO masking.  Each point averages eight states evaluated with 32 independent
  redraws.  Error bars are standard errors across states.}
  \label{fig:ess-crossover}
\end{figure}

\subsection{Formula calibration and held-out decision test}

The direct diagnostic establishes a crossover but uses no decision rule.  We
therefore split the eight checkpoints in every population-ESS interval into
four threshold-fit states and four test states.  The split is stratified by ESS
interval and fixed with seed 161,876.  This gives 28 fit states and 28 test
states with identical interval counts.

At every state, we draw $M=100$ independent base-minibatch blocks, each
containing 128 sequences.  Let $g$ be the exact finite-population gradient,
$G_{W,j}$ the raw gradient from block $j$, and $G_{C,j}$ its paired PPO-masked
gradient.  We estimate the block-level raw covariance trace by
\begin{equation}
  \widehat V_W
  :=\frac1M\sum_{j=1}^M\|G_{W,j}-g\|_F^2.
  \label{eq:formula-raw-variance}
\end{equation}
The raw block is unbiased, so no bias term is added.  For PPO, define
\begin{align}
  \overline G_C
  &:=\frac1M\sum_{j=1}^M G_{C,j},
  \label{eq:formula-ppo-mean}\\
  \widehat V_C
  &:=\frac1{M-1}\sum_{j=1}^M
    \|G_{C,j}-\overline G_C\|_F^2,
  \label{eq:formula-ppo-variance}\\
  \widehat B_C
  &:=\max\left\{0,
    \|\overline G_C-g\|_F^2-\frac{\widehat V_C}{M}\right\}.
  \label{eq:formula-ppo-bias}
\end{align}
The subtraction in Equation~\eqref{eq:formula-ppo-bias} removes the sampling
variance of the estimated mean; truncation at zero enforces a nonnegative
squared-bias estimate.  Averaging $K\in\{1,2,4,8\}$ independent blocks gives
$N=128K$ sequences and the estimated risks
\begin{equation}
  \widehat R_W(K):=\frac{\widehat V_W}{K},
  \qquad
  \widehat R_C(K):=\widehat B_C+\frac{\widehat V_C}{K}.
  \label{eq:formula-risks}
\end{equation}
These are the two sides of Theorem~\ref{thm:crossover} at block level.

For each $K$, the fitted rule applies PPO when
$\rho_{\mathrm{pop}}<\tau_K$ and the raw estimator otherwise.  Candidate
thresholds are zero, the midpoints between adjacent fit-state ESS values, and a
sentinel just above one.  We choose the candidate minimizing the mean of
$\widehat R_C(K)$ or $\widehat R_W(K)$ selected across the 28 fit states.  The
informative estimator size is then chosen using the fit states alone: it must
have $0<\tau_K<1$, so that the rule selects both estimators.  This criterion
selects $K=4$, hence $N=512$.  For comparison, the effective-count transfer
anchors the $K=1$ fit and uses
\begin{equation}
  \tau_K^{\mathrm{count}}:=\min\{1,\tau_1/K\},
  \label{eq:count-transfer}
\end{equation}
which holds the boundary in $N\rho$ fixed.

Held-out evaluation uses an independent stream with seed 271,974.  At each test
state, each of 100 redraws generates eight paired base blocks.  Prefix averages
of one, two, four, and eight blocks form the four estimator sizes, so comparisons
across $N$ share their initial randomness.  Raw and PPO gradients within every
block use the same prompts, responses, rewards, and advantages.  We average
squared gradient error across redraws within a state, then average across the 28
states.  Standard errors use states, not the 2,800 redraws, as the independent
units.  The state-specific component rule in Figure~\ref{fig:formula-oracle}
uses Equation~\eqref{eq:formula-risks} as a diagnostic reference.  The fitted
ESS rule uses the exact population ESS and the threshold learned on the
disjoint fit states; it never sees the test-state gradient errors.  This choice
isolates the quality of the decision boundary from plug-in ESS noise.

The optimization test uses that frozen threshold without further fitting.  A
rollout contains 512 prompts and 16 responses per prompt.  The 512 prompt groups
are partitioned into 16 minibatches, giving 512 sequences per update.  Each
rollout is traversed for four epochs in the same minibatch order, then replaced
by a new rollout; eight rollout cycles produce 512 updates and 65,536 verifier
responses.  We run 100 paired replications.  Within a replication, every method
shares rollout prompts, response-sampling uniforms, and minibatch order.  ESS,
ratios, and the selected gradient are recomputed before every update.  The
gate now uses the observable sample ESS in
Equation~\eqref{eq:sample-ess}, so no population statistic enters training.
The optimization random stream is derived from master seed 424,315.

\subsubsection{On-trajectory shadow diagnostic}

The fixed-$0.1$ run also records a counterfactual diagnostic at every update
with sample ESS below $0.417$, the largest boundary compared in the
optimization study.  At the current policy and on the sampled minibatch, we
compute both the raw gradient and the PPO-masked gradient.  We then enumerate
the finite bandit population to compute the exact current-policy gradient and
record the squared error of each sampled estimator.  These shadow gradients do
not alter the parameter update, which continues to follow the fixed-$0.1$
rule.  Updates above $0.417$ need not be evaluated because every compared gate
uses the raw estimator there.

The diagnostic partitions the recorded updates into
$[0,0.1)$, $[0.1,0.25)$, and $[0.25,0.417)$.  Within each replication, we
compute the number of updates, each estimator's mean squared error, the
fraction of updates on which PPO has lower squared error, and PPO's relative
MSE reduction.  Reported means and standard errors then use the 100
replications as independent units.  The same records include exact one-step
reward changes for both shadow gradients, allowing the MSE certificate to be
distinguished from realized short-step reward.

\subsection{Reproducibility}

The complete implementation is
\texttt{simulation/ess\_policy\_optimization.py}.  When the dataset is absent,
the script downloads the official UCI archive.  It writes the full training,
minibatch, frozen-state, formula-calibration, held-out-evaluation, and
$N=512$ optimization records as CSV files.  It also writes the complete
on-trajectory records, bin summaries, and gate summaries to
\texttt{simulation/results}; exact filenames are listed in the accompanying
README.  The script also writes
Figures~\ref{fig:ess-validation}, \ref{fig:ess-crossover}, and
\ref{fig:formula-oracle} in PDF and PNG formats.  All optimization and exact
gradient calculations use double-precision NumPy on CPU; Matplotlib only
renders the saved results.  The command used for the paper is
\texttt{python simulation/ess\_policy\_optimization.py --replications 100}.

\subsection{Results}

\paragraph{ESS identifies gradient failure.}
Table~\ref{tab:rlvr-diagnostics} reports all prespecified ESS bins.  The
permissive-gradient MSE increases monotonically from the highest-ESS bin to the
lowest.  The corresponding policy step improves reward above ESS $0.2$, is
approximately neutral between $0.1$ and $0.2$, and reduces reward below $0.1$.
In contrast, the oracle-gradient step improves reward in every bin.  The loss
below $0.1$ is therefore attributable to gradient estimation error rather than
to the absence of a locally improving direction.

\begin{figure}[htbp]
  \centering
  \includegraphics[width=\linewidth]{figures/ess_policy_validation.pdf}
  \caption{ESS reliability and the initial operational test.
  (a) Permissive-gradient error by pre-update ESS.
  (b) Exact reward change after a permissive step and after its matched
  oracle-gradient step.  Dotted lines mark the prespecified threshold $0.1$;
  error bars in (a) and (b) are standard errors within ESS bins.
  (c) Relative population-reward improvement against verifier responses;
  bands are 95\% confidence intervals over 100 paired replications.}
  \label{fig:ess-validation}
\end{figure}

\begin{table}[htbp]
\centering
\small
\caption{Minibatch diagnostics from the first 20 replications.  Count is the
number of minibatch updates in each fixed ESS interval.  Gradient MSE and
one-step relative reward changes are means $\pm$ standard errors.}
\label{tab:rlvr-diagnostics}
\begin{tabular}{lrrrr}
\toprule
ESS interval & Count & Raw-gradient MSE & Raw change (\%) & Exact change (\%) \\
\midrule
$[0,0.03)$ & 26 & $0.477\pm0.207$ & $-13.29\pm5.82$ & $6.29\pm1.10$ \\
$[0.03,0.1)$ & 76 & $0.131\pm0.034$ & $-3.99\pm1.75$ & $7.03\pm0.70$ \\
$[0.1,0.2)$ & 137 & $0.054\pm0.008$ & $-0.10\pm0.91$ & $5.94\pm0.48$ \\
$[0.2,0.4)$ & 443 & $0.028\pm0.002$ & $1.19\pm0.22$ & $4.26\pm0.15$ \\
$[0.4,0.6)$ & 585 & $0.022\pm0.001$ & $1.42\pm0.19$ & $3.57\pm0.10$ \\
$[0.6,0.8)$ & 661 & $0.020\pm0.001$ & $1.27\pm0.10$ & $2.99\pm0.07$ \\
$[0.8,1]$ & 632 & $0.016\pm0.001$ & $2.95\pm0.69$ & $3.49\pm0.12$ \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{Sensitivity to estimator construction.}
Table~\ref{tab:rlvr-crossover} reports the direct paired comparison.  With one
training-size minibatch, PPO masking has lower estimated MSE in every ESS
interval.  Averaging four minibatches reduces the permissive estimator's
variance without removing the PPO estimator's bias.  At $N=512$, PPO masking
has lower mean MSE below population ESS $0.4$, while the permissive estimator has
lower mean MSE in the three intervals above $0.4$.  The crossover therefore appears
only after averaging more independent blocks.  This is the batch-size effect
predicted by the factor $N\|b_C\|_2^2$ in
Equation~\eqref{eq:crossover}, applied to the block-level estimator described
above.  The result supports ESS as a crossover coordinate within a fixed update
construction and confirms that the exact oracle boundary is
configuration-specific.  This sensitivity check is not a batch-size
prescription for RLVR: an LLM minibatch count need not equal the number of
independent sampling blocks in the theorem, and the practical $0.1$ rule is
evaluated through its one-sided low-ESS condition instead.

\begin{table}[htbp]
\centering
\small
\caption{Conditional gradient MSE at frozen policy states.  Each entry is the
mean $\pm$ standard error across eight checkpoints after 32 independent
redraws per checkpoint.  PPO reduction is computed per checkpoint; positive
values indicate lower MSE for PPO masking.}
\label{tab:rlvr-crossover}
\begin{tabular}{rrlll}
\toprule
$N$ & Population-ESS interval & Raw MSE & PPO MSE & PPO reduction (\%) \\
\midrule
$128$ & $[0,0.03)$ & $0.085\pm0.028$ & $0.071\pm0.030$ & $25.9\pm11.3$ \\
$128$ & $[0.03,0.1)$ & $0.223\pm0.160$ & $0.182\pm0.164$ & $49.4\pm10.4$ \\
$128$ & $[0.1,0.2)$ & $0.038\pm0.003$ & $0.027\pm0.004$ & $30.1\pm6.6$ \\
$128$ & $[0.2,0.4)$ & $0.031\pm0.007$ & $0.018\pm0.002$ & $33.1\pm7.8$ \\
$128$ & $[0.4,0.6)$ & $0.023\pm0.002$ & $0.019\pm0.003$ & $19.2\pm4.1$ \\
$128$ & $[0.6,0.8)$ & $0.017\pm0.002$ & $0.014\pm0.002$ & $16.4\pm2.4$ \\
$128$ & $[0.8,1)$ & $0.015\pm0.003$ & $0.013\pm0.002$ & $11.1\pm2.8$ \\
\midrule
$512$ & $[0,0.03)$ & $0.049\pm0.020$ & $0.043\pm0.021$ & $26.7\pm9.3$ \\
$512$ & $[0.03,0.1)$ & $0.036\pm0.010$ & $0.031\pm0.011$ & $16.4\pm11.6$ \\
$512$ & $[0.1,0.2)$ & $0.011\pm0.001$ & $0.011\pm0.001$ & $5.1\pm4.9$ \\
$512$ & $[0.2,0.4)$ & $0.007\pm0.001$ & $0.006\pm0.001$ & $9.1\pm6.5$ \\
$512$ & $[0.4,0.6)$ & $0.006\pm0.001$ & $0.007\pm0.001$ & $-19.0\pm5.4$ \\
$512$ & $[0.6,0.8)$ & $0.005\pm0.001$ & $0.005\pm0.001$ & $-17.6\pm4.8$ \\
$512$ & $[0.8,1)$ & $0.004\pm0.001$ & $0.004\pm0.001$ & $-10.5\pm6.5$ \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{Calibration fixes an oracle reference configuration.}
Table~\ref{tab:formula-thresholds} reports every candidate $N$.  The fitted
boundary is above the ESS range for $N=128$ and $N=256$, so these sizes select
PPO at every fit state.  At $N=1024$, the fitted boundary is zero and therefore
selects the raw estimator throughout.  Only $N=512$ yields an interior boundary,
$\tau_4=0.417$, and therefore tests an adaptive rule.  This selection uses no
held-out gradient errors.

\begin{table}[htbp]
\centering
\small
\caption{ESS boundaries fitted from the 28 calibration states.  ``Static''
indicates that the fitted boundary chooses one estimator throughout the
calibration range.}
\label{tab:formula-thresholds}
\begin{tabular}{rrrrl}
\toprule
$N$ & Blocks $K$ & Fitted $\tau_K$ & Count-transfer $\tau_K$ & Fit-state regime \\
\midrule
128  & 1 & $\geq1.00$ & $1.00$ & PPO static \\
256  & 2 & $\geq1.00$ & $0.50$ & PPO static \\
512  & 4 & $0.417$ & $0.25$ & Adaptive \\
1024 & 8 & $0.000$ & $0.125$ & Raw static \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{The calibrated rule improves held-out gradient risk.}
Table~\ref{tab:formula-heldout} reports the relative excess MSE at $N=512$.
The comparison baseline is the better static method on the 28 test states,
which is PPO masking.  Relative differences are computed within each state and
then averaged, so no state with a large gradient scale dominates the reported
percentage.  Both the fitted boundary and the effective-count transfer improve
on the static baseline.  The fixed-$0.1$ row is included only as a reference:
this frozen-state test applies population ESS and optimizes held-out gradient
risk, whereas the heuristic is intended as a sparse trigger based on sample
ESS along a moving training trajectory.  Its estimate is favorable but
imprecise here, so the operational test below addresses the question for which
the heuristic is designed.

\begin{table}[htbp]
\centering
\small
\caption{Held-out estimator selection at $N=512$.  Excess MSE is relative to
the better static estimator and is reported as mean $\pm$ standard error across
28 test states; negative values are better.}
\label{tab:formula-heldout}
\begin{tabular}{lrrr}
\toprule
Decision rule & ESS boundary & Excess MSE (\%) & PPO selections (\%) \\
\midrule
State-specific formula reference & State-specific & $-3.4\pm2.0$ & $50.0$ \\
Fitted ESS & $0.417$ & $-4.3\pm1.1$ & $57.1$ \\
Fixed effective count & $0.250$ & $-4.9\pm1.2$ & $50.0$ \\
Fixed ESS heuristic & $0.100$ & $-1.1\pm2.7$ & $28.6$ \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{The $0.1$ event selects high-risk updates on its own trajectory.}
Table~\ref{tab:on-trajectory-diagnostic} reports the shadow comparison from the
fixed-$0.1$ run.  In the selected region, PPO has lower squared gradient error
on $83.7\%\pm1.4\%$ of updates and reduces conditional MSE by
$53.3\%\pm2.9\%$.  Proposition~\ref{prop:ess-gate-guarantee} therefore
explains the role of this event: switching estimators only there improves the
MSE ordering relative to always using raw.  PPO also has lower MSE on many
updates immediately above $0.1$, so the heuristic is a sparse high-confidence
trigger rather than the full oracle classifier.

\begin{table}[htbp]
\centering
\small
\caption{Shadow estimator comparison along the fixed-$0.1$ trajectory.
Updates per run and paired diagnostic statistics are means $\pm$ standard
errors across 100 replications.  PPO minus raw step is the exact one-step
population-reward difference in percentage points.}
\label{tab:on-trajectory-diagnostic}
\begin{tabular}{lrrrr}
\toprule
Sample ESS & Updates/run & PPO lower MSE (\%) & MSE reduction (\%) &
PPO minus raw step \\
\midrule
$[0,0.1)$ & $16.9\pm0.8$ & $83.7\pm1.4$ & $53.3\pm2.9$ &
$-0.64\pm0.14$ \\
$[0.1,0.25)$ & $31.2\pm1.1$ & $82.7\pm1.3$ & $53.4\pm1.7$ &
$-1.18\pm0.04$ \\
$[0.25,0.417)$ & $26.3\pm1.3$ & $71.2\pm1.3$ & $36.3\pm1.5$ &
$-1.13\pm0.07$ \\
\bottomrule
\end{tabular}
\end{table}

The shadow PPO step has lower immediate reward despite its lower MSE.  This is
not a conflict with Theorem~\ref{thm:mse-improvement}, which orders conservative
lower bounds rather than every realized step.  The diagnostic instead exposes
the intended trade-off: masking gives up some immediate permissive progress to
reduce estimation risk.  The repeated trajectory determines whether that
trade-off prevents later loss.

\paragraph{The adaptive rule improves repeated optimization.}
Table~\ref{tab:n512-optimization} reports the four-epoch $N=512$ study.  The
fitted ESS gate has the highest final reward and the smallest uncertainty.  It
uses the raw estimator for 450.3 of 512 updates on average, which explains its
fast progress, while its 61.7 PPO-masked updates prevent the late loss observed
under the static raw estimator.  The fixed-$0.1$ rule gives a complementary
operating point.  It masks only $3.3\%$ of updates, matches the raw median, and
raises the tenth-percentile endpoint from $0.352$ to $0.495$.  It ends above
the fitted gate in 90 of 100 paired runs, although the fitted gate retains the
higher mean by eliminating a few severe failures that remain under the sparse
rule.  The comparison supports $0.1$ as an efficient practical heuristic
without presenting it as the risk-minimizing boundary.

\begin{table}[htbp]
\centering
\small
\caption{Four-epoch optimization with 512 sequences per update.  Mean final
reward is reported with its standard error over 100 paired replications;
median and tenth percentile are computed across the same final outcomes.}
\label{tab:n512-optimization}
\begin{tabular}{lrrrr}
\toprule
Update rule & Mean final reward & Median & 10th percentile & Masked updates \\
\midrule
Raw & $0.4606\pm0.0098$ & $0.4998$ & $0.3519$ & $0.0$ \\
PPO masked & $0.4038\pm0.0005$ & $0.4036$ & $0.3982$ & $512.0$ \\
Fitted ESS, $\tau=0.417$ & $0.4961\pm0.0003$ & $0.4961$ & $0.4933$ & $61.7$ \\
Fixed count, $\tau=0.250$ & $0.4928\pm0.0050$ & $0.4980$ & $0.4942$ & $35.4$ \\
Fixed ESS, $\tau=0.100$ & $0.4940\pm0.0050$ & $0.4998$ & $0.4954$ & $16.9$ \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{The gate converts reliability into earlier progress.}
Table~\ref{tab:rlvr-path} reports the full optimization trajectory.  The
permissive method learns quickly at first but exhibits greater between-run
uncertainty, whereas PPO masking progresses more slowly through the first half
of training.  The gate follows the permissive path while ESS remains high and
masks only $5.17$ of 128 updates on average.  It consequently leads both static
methods from rollout 3 through rollout 7 and reaches the PPO endpoint at rollout
8.  Since one rollout costs 2,048 verifier responses, the first checkpoint
above 100\% relative improvement occurs after 6,144 responses for the gate,
8,192 for the permissive method, and 10,240 for PPO masking.

\begin{table}[htbp]
\centering
\small
\caption{Relative population-reward improvement after each new rollout batch,
reported as mean $\pm$ standard error over 100 paired replications.}
\label{tab:rlvr-path}
\begin{tabular}{rrrr}
\toprule
Rollout batch & Raw (\%) & PPO mask (\%) & ESS gate (\%) \\
\midrule
0 & $0.00\pm0.00$ & $0.00\pm0.00$ & $0.00\pm0.00$ \\
1 & $46.37\pm2.81$ & $17.94\pm0.40$ & $45.71\pm2.55$ \\
2 & $81.08\pm4.37$ & $39.95\pm0.65$ & $81.72\pm4.27$ \\
3 & $99.17\pm7.24$ & $65.55\pm0.88$ & $109.55\pm5.51$ \\
4 & $129.00\pm7.23$ & $93.88\pm1.18$ & $132.01\pm7.15$ \\
5 & $136.45\pm9.00$ & $124.21\pm1.35$ & $145.78\pm9.39$ \\
6 & $170.32\pm9.37$ & $153.98\pm1.45$ & $174.98\pm9.59$ \\
7 & $189.22\pm9.64$ & $182.20\pm1.46$ & $198.22\pm9.52$ \\
8 & $195.11\pm11.39$ & $208.25\pm1.51$ & $208.51\pm10.26$ \\
\bottomrule
\end{tabular}
\end{table}

The corresponding final exact rewards are $0.2826\pm0.0109$ for the permissive
method, $0.2952\pm0.0014$ for PPO masking, and $0.2954\pm0.0098$ for the gate.
Paired gate differences are $0.0128\pm0.0099$ relative to the permissive method
and $0.0002\pm0.0100$ relative to PPO.  These endpoints do not establish a
statistically resolved final-reward advantage over always masking.  They instead
show that the gate recovers the masked endpoint after following the more
efficient permissive trajectory through the reliable regime.

\clearpage
\section{Proofs for the theoretical results}
\label{app:proofs}

This appendix gives every algebraic step omitted from the main theoretical
development.  The order follows Section~\ref{sec:theory}.

\input{theory_proofs}

\bibliographystyle{plainnat}
\bibliography{references}

\end{document}
