# Writing review: ESS persistence and failure prevention

**Status:** PASS IN SCOPE

## Reverse outline

1. Section 5.1 compares IS and PPO at a fixed Gaussian policy state and
   separates the MSE crossover from the one-step improvement crossover.
2. Section 5.2 identifies sustained low ESS, rather than an isolated crossing,
   as the temporal warning associated with deteriorating policy improvement.
3. Section 6 defines one main conditional template: TIS 3 at high ESS and a
   prespecified clipping safeguard at low ESS.
4. The four-panel 30B result compares three low-support safeguards and then
   exposes the TIS-3 collapse that the conditional template prevents.
5. The unclipped comparison tests the same timing principle without a finite
   TIS cap.
6. The final variant paragraph groups ESS-conditioned skipping across an
   unclipped update and a matched TIS-5 pair, with failure prevention as the
   central TIS-5 result.

## Claim--evidence map

| Claim | Evidence | Status |
|---|---|---|
| Lower MSE does not by itself imply a better fixed-step update | Exact Gaussian IS/PPO moments: MSE crossover at 0.123 and certificate crossover at 0.0392 | Supported |
| Persistent low ESS precedes deteriorating improvement in the two TIS trajectories | Five-point sub-0.01 episodes begin at steps 80 and 68; subsequent evaluations decline | Supported for the reported trajectories |
| An isolated low-ESS observation need not stop improvement | Unclipped GRPO crosses at step 57, immediately recovers, and later peaks at 44.4% | Supported for the reported trajectory |
| ESS-conditioned clipping prevents the observed TIS-3 collapse | Static TIS 3 ends at 0.417%; the TIS-3/Clip-Higher construction ends at 44.8% | Supported |
| The failure-prevention pattern also appears at TIS cap 5 | Static TIS 5 collapses to 0.00%; the matched ESS-skip trajectory ends at 39.4% | Supported for one trajectory per rule |

## Self-review

- **Contribution:** pass. The paper now distinguishes support diagnosis,
  estimator choice, and the operational timing of a safeguard.
- **Clarity:** pass. Each paragraph has one role and Section 6 moves from the
  main TIS-3/clipping template to unclipped clipping and then alternative skip
  actions.
- **Experimental strength:** pass in scope. The reported values are backed by
  the tracked trajectories and use three significant figures.
- **Evaluation completeness:** one limitation remains. Each language-model
  curve is a single training trajectory without across-seed uncertainty.
- **Method soundness:** pass in scope. Section 5's 0.01 descriptive reference
  remains distinct from Section 6's prescribed 0.1 intervention threshold.

## Preservation check

- TIS was removed only from the Gaussian example; the general TIS remark and
  language-model experiments remain intact.
- The unclipped skip variant is no longer mixed into the primary clipping
  paragraph and is discussed with the other ESS-conditioned variants.
- The TIS-5 main result is performance-only; no ESS panel remains.
- No blue feedback markers remain.
