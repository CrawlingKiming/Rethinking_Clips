# Writing review: estimator choices and Gaussian bandit

**Status:** PASS IN SCOPE

## Mini-outline used for the revision

1. Preserve the user's Section 4.1 transition and move from IS reliability to
   the general choice of an alternative estimator.
2. Keep the existing MSE decomposition and fixed-step certificate, but state
   the crossover for an arbitrary coefficient rule rather than PPO alone.
3. Add one concise remark for upper-truncated IS and one for PPO-style masking.
4. Preserve the pairwise IS--PPO oracles in Section 5.1 and overlay matched-cap
   TIS to expose the effect of the estimator construction.
5. Make the Gaussian figure read in the same order as the theory: estimation
   error, retained alignment, and exact one-step improvement.

## Reverse outline

1. Section 4.2 begins with a general decision: after IS reliability weakens,
   compare any controlled coefficient rule with IS.
2. The MSE decomposition identifies the bias--variation crossover shared by
   all such rules.
3. The improvement theorem separates population-gradient alignment from the
   quadratic smoothness penalty.
4. The generalized corollary states exactly when an alternative rule has a
   stronger certificate than IS.
5. The TIS remark identifies upper truncation as a cheap, mild, but limited
   variance safeguard.
6. The PPO remark identifies masking as the stronger intervention and ties its
   use to the certificate crossover.
7. Section 5.1 holds the Gaussian policy state fixed and follows a monotone ESS
   path without Monte Carlo error.
8. The three-panel figure shows how IS, matched-cap TIS, and PPO change MSE,
   retained alignment, and exact policy improvement.

## Claim--evidence alignment

| Claim | Evidence | Scope |
|---|---|---|
| The certificate crossover applies to any alternative coefficient rule | Polarization identity and the generalized proof of Corollary 5 | Common fixed step and common smoothness constant |
| TIS caps the selected upper tail without deleting it, whereas PPO masks saturated terms | Coefficient definitions in Section 4.2 and Appendix A.2 | Structural comparison; no universal dominance claim |
| IS MSE grows sharply as ESS contracts in the Gaussian path | Exact truncated-Gaussian moments in `simulation/gaussian_quadratic_one_step.py` | Stated scalar quadratic construction with (N=32) |
| Matched-cap TIS crosses the IS certificate at (ho=0.093716) | Exact TIS moments and bisection residual check in the simulation script | (c=1.2), (g=2), (eta=0.4) |
| The pairwise IS--PPO MSE, certificate, and PPO-zero boundaries remain (0.122756), (0.039230), and (0.010890) | `simulation/results/gaussian_quadratic_one_step_summary.json` | Pairwise IS--PPO oracle retained from the prior draft |

## Self-review

- The revision is limited to Section 4.2, Section 5.1, their linked proof and
  Gaussian appendix calculation, the Gaussian plotting code, and one dangling
  ESS cross-reference.
- User-authored transitions and edits outside this scope are unchanged; the
  remaining blue Section 6 feedback is intentionally untouched.
- The prose introduces the general estimator choice before naming TIS and PPO,
  and each remark has one distinct message.
- The Gaussian text does not redefine the pairwise IS--PPO oracle after adding
  TIS. TIS is identified explicitly as an overlay with a matched cap.
- ESS is described as a support-regime diagnostic, not as gradient MSE or the
  update oracle.
