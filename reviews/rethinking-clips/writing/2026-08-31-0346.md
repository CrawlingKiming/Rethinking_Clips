# Writing review: completed Section 5

**Status:** PASS IN SCOPE

## Reverse outline

1. The opening states the section's two empirical tasks: an exact one-step
   separation and a temporal effective-support diagnostic.
2. The Gaussian construction fixes the policy state and varies only rollout
   mismatch, making the MSE and certificate comparisons exact.
3. The Gaussian result separates the MSE crossover, the update crossover, and
   the abstention boundary.
4. The language-model subsection defines the observable sample ESS and limits
   its interpretation to importance-weight concentration.
5. The TIS 3 and TIS 5 trajectories establish that support deterioration can
   precede visible evaluation decline.
6. The no-clipping trajectory supplies the complementary case: sustained low
   ESS can coexist with continued policy improvement.
7. The closing assigns ESS one precise role--timing a safeguard--and hands that
   question to Section 6 without claiming that ESS is itself an update oracle.

## Claim--evidence alignment

| Claim | Evidence | Scope |
|---|---|---|
| PPO can have lower MSE while IS gives the better one-step update | Exact moments and expected gains from `simulation/gaussian_quadratic_one_step.py` | Exact for the stated scalar Gaussian construction |
| The safe oracle has IS, PPO, and null-update regimes | Boundaries in `simulation/results/gaussian_quadratic_one_step_summary.json` | Configuration-specific for $N=32$, $\epsilon=0.2$, $g=2$, and $\eta=0.4$ |
| ESS deterioration precedes the next observed decline in the two TIS runs | Per-step traces underlying Figure 1: crossings at steps 66 and 62, followed by lower evaluations at step 70 | Trajectory-level temporal evidence |
| Low ESS does not imply immediate failure | No-clipping run: sustained low-support onset at step 99; 34/41 ESS observations below 0.1 through the step-140 peak while AIME rises from 39.8% to 44.4% | One trajectory; no across-run inference or estimator ranking is claimed |

## Style and terminology

- The subsection follows the author's Gaussian-to-LLM blueprint and preserves
  the causal transitions between the two examples.
- Visible estimator terminology uses `IS`, not `Raw`.
- The language-model evidence is organized around one question per paragraph;
  definitions, evidence, and implications are not mixed.
- Gradient norm has been removed from the argument, caption, and figure.
- The new figure reuses the motivation figure's black-performance/red-ESS
  visual grammar and avoids redundant TIS panels already shown in Figure 1.

## Scope check

Section 5 does not claim that sample ESS estimates full gradient MSE, predicts
failure deterministically, or selects the better estimator. It supports ESS as
an observable effective-support diagnostic and motivates testing an ESS-timed
safeguard in Section 6.
