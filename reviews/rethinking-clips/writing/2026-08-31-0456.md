# Writing review: completed Section 6

**Status:** PASS IN SCOPE

## Reverse outline

1. The opening converts Section 5's diagnostic result into an operational
   question: whether ESS can time a prespecified safeguard.
2. The update rule separates the timing variable, ESS, from the low-support
   action, and fixes the language-model threshold at 0.1.
3. The protocol defines the 30B RLVR task, permissive and safeguarded branches,
   raw versus shaped ESS, and the single-trajectory scope.
4. The main 30B comparison establishes permissive early learning and stronger
   late retention under ESS-conditioned control.
5. The unclipped family holds the coefficient update fixed and compares the
   low-ESS actions directly.
6. The realized intervention schedule verifies that intervention frequency
   increases as effective support contracts.
7. The 8B and RLHF comparisons test whether the same construction remains
   useful after changing model scale and objective.
8. The closing assigns ESS one role: locating the reliability regime while a
   prespecified safeguard determines the update.

## Claim--evidence alignment

| Claim | Evidence | Scope |
|---|---|---|
| ESS-conditioned control rescues the reported 30B DPPO and TIS trajectories | AIME-2024 trajectories in `dppo_base.csv`, `dppo_ess.csv`, `cispo3_nogate.csv`, and `dapo_ess.csv` | One trajectory per curve; the TIS/Clip-Higher panel is a complete-recipe comparison |
| Conditional clipping retains most early performance and improves late retention in the unclipped family | Common-update runs `noclip_ungated.csv`, `noclip_ess_clip.csv`, and `noclip_ess_skip.csv` | Direct intervention-rule comparison; the reference has three nominal fraction-trigger interventions |
| Intervention intensifies as support contracts | DPPO raw ESS mean 0.412 to 0.120 and `trip` mean 0.153 to 0.668; Appendix Figure 8 | Descriptive trajectory evidence, not a calibrated oracle claim |
| The endpoint ordering transfers to Qwen3-8B | Aggregation-matched Clip-Higher and DPPO pairs | Complete conditional constructions; gains are method-specific |
| The optimization pattern transfers to an open-ended RLHF objective | GRPO versus TIS-3-plus-ESS training reward-model score and response length | Full-recipe comparison on the training reward model, not human preference |

## Style and terminology

- The section follows the paper's theory-to-example-to-optimization sequence.
- Each paragraph has one job: rule, protocol, result, mechanism, or transfer.
- ESS is consistently described as a support coordinate and timing heuristic,
  not as full gradient MSE or an update oracle.
- Band-matched and full-recipe comparisons are named directly, without using
  them as gate-only ablations.
- Gradient norm and redundant diagnostic panels are absent from the main
  figures.

## Scope check

The section explicitly states that each curve is a single training trajectory
without across-seed uncertainty. Shaped ESS is disclosed where used. The RLHF
result is limited to training reward-model optimization, and the threshold 0.1
is identified as a prespecified language-model setting rather than a universal
or Gaussian-derived cutoff.
