# Writing review: Section 5 policy optimization and ESS

**Status:** PASS IN SCOPE

## Blueprint and argument flow

- The revision starts from the author's pushed `771591d` blueprint and preserves
  its order: exact Gaussian analysis, language-model diagnostics, then the ESS
  gate in Section 6.
- The Gaussian subsection first fixes the policy and varies rollout mismatch,
  then defines the MSE and certificate decisions, and finally reports the exact
  separation between their crossover points.
- The language-model subsection moves from population ESS to its sample
  diagnostic, aligns ESS and gradient norm with validation, and ends by stating
  the precise role that ESS carries into Section 6.

## Claim--evidence alignment

| Claim | Evidence | Scope |
|---|---|---|
| PPO can have lower estimator MSE while IS gives the better update | Exact Gaussian moments and one-step gains from `simulation/gaussian_quadratic_one_step.py` | Exact for the stated scalar quadratic construction |
| The safe certificate oracle has IS, PPO, and abstention regimes | Exact boundaries in `simulation/results/gaussian_quadratic_one_step_summary.json` | Configuration-specific for $N=32$, $\epsilon=0.2$, $g=2$, and $\eta=0.4$ |
| ESS deterioration precedes validation deterioration in the two TIS trajectories | Per-step CSV traces in `only_for_figures/data` and regenerated Figure 3 | Trajectory-level diagnostic |
| Low ESS alone does not determine update quality | The no-clip trajectory improves after crossing the same ESS threshold | No deterministic threshold or between-run inference is claimed |

## Terminology and figures

- Visible estimator terminology uses `IS`, not `Raw`.
- ESS is described as a diagnostic of effective support, not as the full
  gradient MSE or a stand-alone update oracle.
- Gaussian panel (b) is titled `Policy improvement` and visibly marks the MSE,
  certificate, and abstention boundaries.
- The former pooled-correlation figure was replaced because between-run
  gradient-scale differences confounded that comparison. The replacement shows
  the supported temporal evidence directly.

## Remaining scope

The language-model panel contains one trajectory per configuration. Section 5
therefore treats it as a temporal diagnostic and does not claim across-run
statistical inference.
