# Writing review: exact Gaussian one-step reconstruction

**Status:** PASS IN SCOPE

## Section outline

1. Section 4.4 first separates estimator risk from the full fixed-step certificate.
2. The exact Gaussian bandit derives normalized ESS, Raw/PPO-style moments, MSE, and expected one-step gain from one-dimensional Gaussian integrals.
3. The example reports the persistent MSE crossover, the later certificate crossover, and the no-update boundary for `N=32`.
4. The language-model section tests the temporal implication at scale without claiming access to the population oracle.
5. Optdigits is retained only as auxiliary finite-population validation in Appendix A.

## Reverse outline of the revised Section 4.4

- **Estimator comparison:** define bias, variance, MSE, update mean, and update second moment.
- **Full certificate:** show why MSE alone drops a nonnegative second-moment term and can rank updates incorrectly.
- **Oracle definition:** distinguish the MSE oracle, certificate oracle, and safe certificate oracle.
- **Exact example:** derive the Gaussian ratio, `rho=exp(-delta^2)`, advantage, PPO-style mask, and closed-form moments.
- **Evidence:** report `rho_MSE=0.122756`, `rho_B=0.039230`, and `rho_0=0.010890` on the stated branch.
- **Interpretation:** high ESS removes the mismatch-driven rationale for aggressive masking; low ESS is diagnostic but is not itself proof that PPO is preferable.
- **Practical bridge:** define sample ESS and state that the LLM threshold `0.1` is prespecified rather than transferred from the Gaussian example.

## Claim--evidence map

| Claim | Evidence | Status |
|---|---|---|
| Normalized ESS is exactly `exp(-delta^2)` in the Gaussian pair. | Equation `gaussian-ratio-ess`; direct Gaussian second-moment calculation. | Supported |
| PPO can have lower MSE while Raw has larger one-step improvement. | Closed-form interval `0.039230 < rho < 0.122756`; at `rho=0.0773`, MSE is `6.468` vs. `3.557`, while gain is `0.763` vs. `0.098`. | Supported |
| The full certificate equals actual expected improvement in the example. | Quadratic objective with Hessian `-1`; Equation `gaussian-exact-gain`. | Supported |
| The safe one-step oracle has an ESS-threshold representation on the displayed branch. | `rho=exp(-delta^2)` is one-to-one and the closed-form certificate has the reported Raw--PPO--no-update ordering. | Supported on the stated branch |
| The numerical thresholds are not universal. | Their definitions depend explicitly on `g`, `N`, `epsilon`, and `eta`; the limitation is stated in Section 4.4 and the conclusion. | Supported |
| Optdigits is not required for the main oracle claim. | The main derivation is analytic; Optdigits figures and protocol are moved to Appendix A and labeled auxiliary. | Supported |

## Five-dimension self-review

1. **Contribution: pass.** The non-obvious result is the separation between lower estimator MSE and a stronger policy update.
2. **Writing clarity: pass.** Each quantity is defined before use, and the exact example follows the theorem in execution order.
3. **Experimental strength: pass in scope.** The main example has no Monte Carlo error; large-model evidence remains trajectory-level and is described accordingly.
4. **Evaluation completeness: pass in scope.** Exact calculation code, CSV output, figure, auxiliary Optdigits evidence, and LLM evidence are all retained.
5. **Method soundness: pass with stated scope.** The example uses an exact current-policy value baseline and a PPO-style advantage-dependent mask. The branch and configuration dependence are explicit.

## Remaining scope limitations

- The exact thresholds belong to the scalar Gaussian construction and do not calibrate the LLM threshold.
- The language-model study does not observe population gradient alignment or the exact full certificate.
- A practical plug-in certificate selector still requires online estimation of alignment and second moments.
