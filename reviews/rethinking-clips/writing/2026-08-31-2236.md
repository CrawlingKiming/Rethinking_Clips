# Writing review: Figure 3 and completed Section 6

**Status:** PASS IN SCOPE

## Reverse outline

1. Section 5.1 fixes the policy state and separates estimator MSE, retained
   alignment, and exact one-step improvement in the Gaussian bandit.
2. TIS with cap 3 shows that coefficient control can retain substantially more
   signal than PPO masking even when both reduce variance.
3. Section 5.2 places the two motivating TIS failures beside unclipped GRPO,
   showing that a sub-0.01 ESS observation identifies weak support but is not a
   failure certificate by itself.
4. Section 6 defines ESS as the timing variable and keeps the low-support
   safeguard as a separate design choice.
5. The four-panel 30B comparison reports GRPO, Clip-Higher, DPPO, and TIS 3,
   with one paragraph devoted to each result.
6. The unclipped family isolates the low-ESS action, while TIS 5 supplies a
   gate-only replication with an ESS-conditioned skip.
7. The 8B and RLHF experiments test transfer across model scale and objective;
   the closing paragraph returns to the division between timing and control.

## Claim--evidence map

| Claim | Evidence | Status |
|---|---|---|
| TIS cap 3 and PPO cross IS at different support levels | Exact Gaussian moments and regenerated Figure 2; substantive TIS crossover 0.105, IS--PPO crossover 0.0392 | Supported |
| A sub-0.01 ESS observation is diagnostic but not a failure certificate | Figure 3: TIS failures cross at steps 77 and 68; unclipped GRPO has a transient crossing at 57 and later peaks at 44.4% | Supported for the reported trajectories |
| ESS-conditioned recipes improve the reported 30B endpoints | Figure 4 and CSV-backed GRPO, Clip-Higher, DPPO, and TIS-3 trajectories | Supported; panels (a)--(b) are complete-recipe comparisons |
| The TIS-5 rescue is a gate-only replication | Static and ESS-skip TIS-5 trajectories in Figure 6 | Supported for one trajectory per rule |
| The construction transfers to the reported RLHF optimization objective | Reward-model score in Figure 8, 43.0 versus 60.7 | Supported for training reward, not human preference |

## Self-review

- **Contribution:** pass. The text distinguishes reliability, estimator choice,
  and operational timing rather than treating ESS as the oracle.
- **Clarity:** pass. Each requested Section 6 comparison now has its own
  paragraph and the figure caption states which comparisons are recipe-level.
- **Experimental strength:** pass in scope. All numbers were checked against the
  tracked CSVs and reported to three significant figures.
- **Evaluation completeness:** limitation disclosed. Each large-model curve is
  one training trajectory without across-seed uncertainty.
- **Method soundness:** pass in scope. The 0.01 diagnostic reference in Section
  5 remains distinct from the prespecified 0.1 intervention gate in Section 6.

## Terminology and preservation check

- User-authored material outside the requested Figure 3, Gaussian TIS setting,
  and Section 6 feedback was left unchanged.
- No blue feedback markers remain.
- IS, TIS, PPO, raw ESS, and shaped ESS retain distinct meanings.
- Numerical results and plotted endpoint labels use three significant figures.
