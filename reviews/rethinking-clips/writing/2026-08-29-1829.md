# Writing review: Section 3 blueprint polish

**Status:** PASS IN SCOPE

## Why the blueprint is stronger

The previous draft was organized as a sequence of definitions. The user's
rewrite is organized as an argument: population gradient, finite-sample gap,
estimator choice, clipping tradeoff, then R\'enyi divergence and ESS. Each new
object now appears because the preceding paragraph creates a need for it.

Good exposition for this section therefore follows three rules:

1. State the statistical problem before introducing the diagnostic.
2. Let each definition answer the question raised immediately before it.
3. Use direct claim--consequence transitions instead of scope narration or
   repeated qualifications.

## Revision scope

The user's headings, paragraph order, and causal scaffold were preserved.
Edits were limited to grammar, missing definitions, citations, and mathematical
precision. In particular, the revision defines the Raw coefficient, specifies
the rollout sampling law, identifies PPO's masking coefficient, and states ESS
as the policy-mismatch factor in a bounded Raw-MSE envelope rather than as the
full gradient MSE.

## Reverse outline

1. Define the population policy gradient under the rollout distribution.
2. Expose the gap between that identity and its finite-rollout estimate.
3. Place Raw and clipped estimators in one coefficient-rule notation.
4. State the variance reduction versus lost-signal tradeoff.
5. Introduce R\'enyi divergence and normalized ESS as the reliability measure
   demanded by that tradeoff.

No structural rewrite or claim expansion was introduced.
