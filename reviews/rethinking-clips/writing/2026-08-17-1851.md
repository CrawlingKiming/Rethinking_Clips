# Contextual-bandit section reorganization self-review

## Claim-evidence map

| Claim | Evidence shown in the main text | Detailed support | Status |
|---|---|---|---|
| ESS predicts failure of the permissive gradient | Figure 2(a) reports gradient MSE by ESS and Figure 2(b) reports the exact one-step reward consequence | Appendix A.5 defines the exact gradient, ESS bins, counterfactual steps, and aggregation | Supported in the contextual-bandit task |
| The preferred estimator depends jointly on ESS and estimator size | Section 4.3 states the N = 128 boundary case and the N = 512 crossover; Figure 3(a,b) shows the fitted boundary across N | Appendix Figure 5 and Table 4 report the complete paired MSE diagnostic | Supported in the controlled task |
| An ESS gate can improve policy optimization | Figure 2(c) reports the fixed 0.1 heuristic and Figure 3(d) reports the calibration-selected gate | Appendix A.7 specifies threshold fitting, held-out testing, and the repeated optimization loop | Supported for the two reported configurations |
| The value 0.1 follows from the theorem | The main text labels 0.1 as a prespecified application heuristic and separates it from the fitted 0.42 boundary | The held-out risk result for fixed 0.1 remains statistically unresolved | Explicitly rejected |
| The contextual-bandit result transfers to RLVR | Section 5 specifies the matched LLM experiment and retains placeholders | No completed LLM result is claimed | Open |

## Contribution

1. **Pass.** Section 4 is now framed as a contextual-bandit policy-optimization experiment, not as a generic simulation.
2. **Pass.** Its role is explicit: validate the reliability mechanism, convert the MSE crossover into an observable gate, and test the optimization consequence.
3. **Pass.** The section connects the exact-oracle study to the later RLVR experiment without treating the controlled task as language-model evidence.
4. **Pass in scope.** The direct crossover diagnostic remains available but no longer competes with the decision-rule result in the main narrative.
5. **Open for transfer.** The paper still needs completed matched LLM runs before it can make the cross-domain claim planned in Section 5.

## Writing clarity

1. **Pass.** The section opens with three ordered questions that match the theory and determine the subsection order.
2. **Pass.** The testbed is motivated before its mechanics: an LLM cannot expose the population gradient, whereas the finite contextual bandit can.
3. **Pass.** Figure 2 carries the prespecified heuristic result; Figure 3 carries calibration, held-out risk, and repeated optimization.
4. **Pass.** The final paragraph states exactly what the experiment establishes and separates the fitted 0.42 boundary from the 0.1 application heuristic.
5. **Pass.** The transition to Section 5 now states what must be tested once the exact gradient is unavailable.

## Experimental strength

1. **Pass in scope.** The task preserves sequence ratios, grouped responses, repeated minibatch updates, and rollout refresh while allowing exact population evaluation.
2. **Pass.** The main text reports both estimator reliability and policy optimization, rather than presenting MSE as an end in itself.
3. **Pass.** The calibration rule is fixed without held-out outcomes, and boundary cases at N = 128, 256, and 1024 remain reported.
4. **Pass.** The optimization claims retain the original effect sizes, uncertainty, pairing, and intervention frequency.
5. **Open for external validity.** Optdigits is one finite task and one policy class; it cannot establish universal thresholds or universal dominance.

## Evaluation completeness

1. **Pass.** The main section exposes the results needed for the paper's core argument and points to the full diagnostic rather than omitting it.
2. **Pass.** The appendix defines the task, exact gradient, sample and population ESS, redraw sizes, seeds, independent units, threshold candidates, and optimization protocol.
3. **Pass.** Figure 3(c) compares each adaptive rule against the better static estimator on held-out states.
4. **Pass.** Figure 3(d) reports a full repeated-optimization trajectory rather than a single checkpoint.
5. **Open.** The prespecified 0.1 rule is useful in Figure 2(c), but its held-out MSE advantage in Figure 3(c) is unresolved; the text states this limitation directly.

## Method design soundness

1. **Pass.** ESS is presented as a reliability coordinate conditional on estimator size, not as the full gradient MSE.
2. **Pass.** PPO clipping remains an advantage-sign-dependent gradient mask; no ratio-truncation claim is introduced by this reorganization.
3. **Pass.** The main claims follow the theory in order: effective support, gradient error, estimator crossover, and policy consequence.
4. **Pass.** No numerical result was changed during the reorganization.
5. **Pass in scope.** The exact-oracle task supports mechanism validation, while Section 5 correctly reserves transfer for matched LLM evidence.

## Reviewer-facing conclusion

The revised section now has a single function in the paper. It uses a contextual bandit because exact gradients are required to test the theory, shows that ESS predicts permissive-gradient failure, demonstrates the ESS-and-N estimator crossover, and tests whether a boundary fixed before evaluation improves policy optimization. The fixed 0.1 rule remains a practical bridge to RLVR, not a theorem-derived constant. The direct crossover plot and full protocol remain in the appendix, where they support rather than interrupt the main argument.
