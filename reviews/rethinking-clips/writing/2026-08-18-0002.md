# Convergence theorem and ratio-truncation self-review

## Revised section outline

1. Define mean-square first-order stationarity as the convergence target.
2. State the finite-time guarantee directly as optimization error plus average gradient MSE.
3. Put exact convergence, finite-sample complexity, and the persistent-error neighborhood inside the theorem statement.
4. Move the exact initialization-gap constant to the line-by-line appendix proof.
5. Separate estimator finite-MSE requirements from parameter boundedness and projection.
6. Connect upper-ratio truncation to the convergence theorem through full MSE rather than variance alone.
7. Reiterate that PPO clipping is advantage-sign gradient masking, not numerical ratio truncation.

## Paragraph roles and reverse outline

| Role | Main message | Handoff |
|---|---|---|
| Convergence target | Nonconcave policy optimization uses first-order stationarity as its standard finite-time target. | State the convergence guarantee in that metric. |
| Main theorem | The expected squared gradient is at most an initialization term that vanishes as $1/T$ plus average gradient MSE. | Interpret the vanishing and persistent-MSE regimes. |
| Interpretation | Vanishing MSE gives exact stationarity, while persistent MSE gives only a stationary neighborhood. | Attribute the generic argument and identify the paper-specific term. |
| Assumptions | Finite estimator MSE is distinct from bounded parameters or projection. | Ask whether an estimator safeguard can restore finite MSE. |
| Ratio truncation | Upper truncation can make an otherwise vacuous expected bound finite, but it improves the certificate only when full MSE decreases. | Preserve the clipping-versus-truncation distinction. |
| Decision logic | ESS identifies reliability, while any safeguard is justified by its full bias-variance tradeoff. | Proceed to the observable training rule. |

## Claim-evidence map

| Claim | Evidence | Status |
|---|---|---|
| Vanishing average gradient MSE implies convergence to first-order stationarity | Theorem 3, Equation (21), and the line-by-line proof in Appendix C.3 | Supported |
| A persistent MSE level yields only a stationary neighborhood | Theorem 3 limit-superior statement and Appendix C.3 | Supported |
| The exact objective-gap constant is not necessary in the reader-facing convergence statement | Theorem 3 uses $C_{\mathrm{init}}/T$; Appendix C.3 gives its exact value | Supported and transparent |
| No bounded parameter set or projection is required for the unprojected theorem | Assumptions paragraph after Theorem 3; projected-gradient exception cited to Xiao | Supported within stated smoothness and feasibility conditions |
| Upper-ratio truncation restores finite MSE when the raw weighted gradient has infinite second moment and $H_\theta$ is bounded | Corollary 11 and the following convergence discussion | Supported |
| Finite variance alone makes truncation preferable | Explicitly rejected; Theorem 3 depends on full MSE including truncation bias | Rejected and clarified |
| PPO clipping guarantees finite estimator variance | Explicitly rejected because its advantage-sign mask does not cap every retained ratio | Rejected and clarified |
| The conditional ESS substitution covers arbitrary fixed-rollout reuse | Explicitly not claimed | Requires an adaptive-data extension |

## Five-dimension skeptical review

1. **Contribution: pass in scope.** The theorem now exposes the estimator-dependent convergence term immediately and reserves the standard initialization constant for the proof.
2. **Writing clarity: pass.** A reader can distinguish exact convergence, stationary-neighborhood convergence, and nonclaims about parameters or global optimality from the theorem statement alone.
3. **Experimental strength: unchanged.** No empirical value or interpretation was altered.
4. **Evaluation completeness: open outside scope.** Fixed-rollout adaptive dependence and multi-seed LLM uncertainty remain separate open issues.
5. **Method soundness: pass with qualification.** Truncation is justified only through lower full MSE. The text does not infer infinite gradient variance from an infinite ratio moment alone.

## Reviewer-facing conclusion

**Verdict: PASS IN SCOPE.** The main result now reads as a finite-time convergence guarantee, not as an objective-improvement rearrangement. The theorem states the vanishing-MSE convergence conclusion directly, while the appendix preserves the exact constant and every proof step. The truncation discussion correctly identifies the infinite weighted-gradient-moment case in which a cap makes the expected guarantee nonvacuous, without claiming that finite variance or a fixed cap is sufficient for better asymptotic convergence.
