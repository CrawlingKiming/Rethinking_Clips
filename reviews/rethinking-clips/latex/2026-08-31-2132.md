# LaTeX and visual review: estimator choices and Gaussian bandit

**Status:** SHIP

## Reproduction and build

- `python -m py_compile simulation/gaussian_quadratic_one_step.py`: passed.
- `python simulation/gaussian_quadratic_one_step.py`: passed, including all
  crossover residual and displayed-branch dominance checks.
- `latexmk -pdf -interaction=nonstopmode -halt-on-error main.tex`: passed.
- Output: 23 pages, with the compiled copy published to `main.pdf`.
- Undefined citations or references: 0.
- Overfull boxes: 0.
- Remaining diagnostic: one benign underfull bibliography line in the existing
  Pirotta et al. entry.

## Figure verification

- Regenerated `figures/gaussian_quadratic_one_step.pdf` and its PNG preview from
  the tracked exact-moment script.
- Verified the generated figure PDF with `pdfinfo` before compilation.
- The new figure uses the paper's shared font, line, grid, and color system.
- Panel (a) shows exact gradient MSE, panel (b) retained alignment, and panel
  (c) the exact certificate/one-step gain for IS, TIS, and PPO.
- The low-support PPO zero boundary is marked at (ho=0.010890), rather than
  by a generic (0.1) guide.

## Visual inspection

- Rendered and inspected manuscript pages 6--8 at 144 dpi.
- The generalized corollary fits without overflow, and both remarks remain
  concise and visually separated.
- The three-panel Gaussian figure is legible at manuscript size; titles,
  axes, threshold labels, legend, and caption do not overlap or clip.
- Section 5.1 flows onto the figure page without an orphaned heading or broken
  caption.
