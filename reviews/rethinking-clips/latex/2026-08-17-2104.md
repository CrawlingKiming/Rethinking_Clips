# LaTeX compilation and PDF verification

## Compilation status

| Field | Value |
|---|---|
| Status | Clean |
| Iterations | 2 compilation passes after the final edit |
| Pages | 32 |
| LaTeX or package warnings | 0 |
| Unresolved citations or references | 0 |
| Overfull boxes | 0 |
| Underfull boxes | 0 |
| Citation audit | 20 cited keys, 21 bibliography keys, no missing keys, one unused key |
| PDF integrity | Root and out PDFs have identical SHA-256 hashes |

The only unused bibliography entry is `dudik2011doubly`, or 4.8% of the bibliography, below the rubric threshold for an unused-entry deduction. The project `.latexmkrc` is byte-identical to the installed canonical configuration. The local `latexmk` launcher was unavailable, so the canonical pdfLaTeX, BibTeX, pdfLaTeX, pdfLaTeX sequence was run directly with all intermediate files in `out/`.

## Visual verification

Pages 5 through 8 and 26 through 30 were rendered and inspected at 140 DPI. The new theorem and both corollaries fit within the text block, equations are legible, theorem text does not collide with page boundaries, and the line-by-line proofs remain continuous. No clipped text, overlap, broken glyph, missing figure, excessive blank page, footer collision, or unreadable equation was found.

## Source pathologies

Three intentional `[H]` floats remain, all attached to placeholders or comparison blocks whose local placement is useful. No manual vertical spacing, shrink-to-fit object, tiny-table hack, forced semantic line break, fixed-width layout assumption, absolute positioning, or label-before-caption error was found. `chktex` and `latexindent` are not installed and were skipped. No em dash or `\boxed` command appears in the paper sources.

## Quality score

| Metric | Value |
|---|---|
| Score | 99 / 100 |
| Verdict | Ship |

### Deductions

| # | Issue | Tier | Deduction | Category |
|---|---|---|---|---|
| 1 | Three intentional `[H]` floats | Minor | -1 | Source style |
| | **Total deductions** | | **-1** | |
